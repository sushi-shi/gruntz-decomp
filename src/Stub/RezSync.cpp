// RezSync.cpp - GRADUATED. RezSync::Init (0x00083450, 6445 B) is now
// reconstructed as a full /GX EH body in src/Gruntz/RezSync.cpp (the `rezsync`
// eh unit in config/units.toml). This file no longer carries an RVA; it is kept
// only so All.cpp's `#include "RezSync.cpp"` stays valid.
