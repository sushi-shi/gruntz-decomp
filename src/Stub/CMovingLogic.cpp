#include <rva.h>
// CMovingLogic.cpp - engine-label stubs for CMovingLogic.
//
// The ctor (0x13940) is reconstructed in src/Gruntz/CMovingLogicCtor.cpp (its own
// TU so the standalone out-of-line copy can be emitted without regressing
// CProjectile's folded inline copy). Nothing left to stub here.
