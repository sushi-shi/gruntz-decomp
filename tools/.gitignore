# Build artefacts: the Rust target dir and the wine-built recomp harness.
# `tools/recomp/pidrun.exe` is regenerated by tools/recomp/build.sh; committing
# a binary that maps retail code would be both large and pointless.
/target/
/recomp/pidrun.exe
/recomp/*.obj
