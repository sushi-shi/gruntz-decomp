{
 "schema": 1,
 "source": "src/Dsndmgr/DirectSoundMgr.cpp",
 "rva": "0x135d70",
 "axes": [
  {
   "name": "guard_form",
   "find": "DirectSoundMgr* DSoundCloneInst::GetItem() {\n    if (!m_owner->m_initialized) {\n        return 0;\n    }\n    CloneNode* node = static_cast<CloneNode*>(m_cloneList.m_head);",
   "options": [
    {
     "name": "baseline"
    },
    {
     "name": "eq_zero",
     "replace": "DirectSoundMgr* DSoundCloneInst::GetItem() {\n    if (m_owner->m_initialized == 0) {\n        return 0;\n    }\n    CloneNode* node = static_cast<CloneNode*>(m_cloneList.m_head);"
    },
    {
     "name": "owner_local",
     "replace": "DirectSoundMgr* DSoundCloneInst::GetItem() {\n    SoundDevice* owner = m_owner;\n    if (!owner->m_initialized) {\n        return 0;\n    }\n    CloneNode* node = static_cast<CloneNode*>(m_cloneList.m_head);"
    },
    {
     "name": "node_decl_first",
     "replace": "DirectSoundMgr* DSoundCloneInst::GetItem() {\n    CloneNode* node;\n    if (!m_owner->m_initialized) {\n        return 0;\n    }\n    node = static_cast<CloneNode*>(m_cloneList.m_head);"
    }
   ]
  }
 ]
}