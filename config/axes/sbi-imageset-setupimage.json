{
 "schema": 1,
 "source": "src/Gruntz/SBI_ImageSet.cpp",
 "rva": "0xe72f0",
 "axes": [
  {
   "name": "entry_guard",
   "find": "    static_cast<void>(extra);\n    // ONE `||`: retail sends host, owner AND the record guard to a single shared\n    // `xor eax,eax; pop esi; ret 0x2c`. (Two separate `if`s inline all four exits\n    // and cost a point - the OPPOSITE of the sibling CSBI_ImageSetAni::Init, whose\n    // retail shape keeps a private epilogue per guard. Same site, different optimum:\n    // docs/patterns/same-sites-different-per-function-optimum.md.)\n    if (host == 0 || owner == 0) {\n        return 0;\n    }\n    m_2c = owner;",
   "options": [
    {
     "name": "baseline_or"
    },
    {
     "name": "not_or",
     "replace": "    static_cast<void>(extra);\n    if (!host || !owner) {\n        return 0;\n    }\n    m_2c = owner;"
    },
    {
     "name": "found_hoisted",
     "replace": "    static_cast<void>(extra);\n    CObject* found;\n    if (host == 0 || owner == 0) {\n        return 0;\n    }\n    m_2c = owner;"
    },
    {
     "name": "host_local",
     "replace": "    static_cast<void>(extra);\n    CDDrawSurfaceMgr* h = host;\n    if (h == 0 || owner == 0) {\n        return 0;\n    }\n    m_2c = owner;"
    },
    {
     "name": "extra_last",
     "replace": "    if (host == 0 || owner == 0) {\n        return 0;\n    }\n    static_cast<void>(extra);\n    m_2c = owner;"
    },
    {
     "name": "no_extra_stmt",
     "replace": "    if (host == 0 || owner == 0) {\n        return 0;\n    }\n    m_2c = owner;"
    }
   ]
  },
  {
   "name": "lookup_block",
   "find": "    CObject* found = 0;\n    host->m_imageRegistry->m_10map.Lookup(key, found);\n    CDDrawWorker* rec = static_cast<CDDrawWorker*>(found);\n    m_34 = rec;\n    if (rec == 0) {\n        return 0;\n    }",
   "options": [
    {
     "name": "baseline"
    },
    {
     "name": "registry_local",
     "replace": "    CObject* found = 0;\n    CDDrawWorkerRegistry* reg = host->m_imageRegistry;\n    reg->m_10map.Lookup(key, found);\n    CDDrawWorker* rec = static_cast<CDDrawWorker*>(found);\n    m_34 = rec;\n    if (rec == 0) {\n        return 0;\n    }"
    },
    {
     "name": "via_member",
     "replace": "    CObject* found = 0;\n    m_24->m_imageRegistry->m_10map.Lookup(key, found);\n    CDDrawWorker* rec = static_cast<CDDrawWorker*>(found);\n    m_34 = rec;\n    if (rec == 0) {\n        return 0;\n    }"
    }
   ]
  },
  {
   "name": "frame_resolve",
   "find": "    i32 f = frame;\n    if (f == -1) {\n        f = rec->m_minIndex;\n    }\n    m_38 = f;",
   "options": [
    {
     "name": "baseline"
    },
    {
     "name": "ternary",
     "replace": "    i32 f = (frame == -1) ? rec->m_minIndex : frame;\n    m_38 = f;"
    }
   ]
  }
 ]
}
