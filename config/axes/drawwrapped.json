{
 "schema": 1,
 "source": "src/Font/Font.cpp",
 "rva": "0x17a460",
 "axes": [
  {
   "name": "first_extent",
   "find": "        TextExtent e = MeasureText(text);\n        if (e.width + x <= rc.right && !nl) {\n",
   "options": [
    {
     "name": "copy"
    },
    {
     "name": "split",
     "replace": "        TextExtent e = MeasureText(text);\n        i32 eW = e.width;\n        if (eW + x <= rc.right && !nl) {\n"
    },
    {
     "name": "direct",
     "replace": "        if (MeasureText(text).width + x <= rc.right && !nl) {\n"
    }
   ]
  },
  {
   "name": "breakword",
   "find": "            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < text.GetLength()) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < text.GetLength() && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head;\n            if (breakNL) {\n                head = text.Left(i);\n            } else {\n                head = text.Left(i + 1);\n            }\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            TextExtent he = MeasureText(head);\n            i32 headW = he.width;\n            text = text.Right(text.GetLength() - i - 1);\n",
   "options": [
    {
     "name": "scan_len=fresh-nl_len=fresh-head_extent=copy-right_count=fresh"
    },
    {
     "name": "scan_len=fresh-nl_len=fresh-head_extent=copy-right_count=outerlen",
     "replace": "            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < text.GetLength()) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < text.GetLength() && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head;\n            if (breakNL) {\n                head = text.Left(i);\n            } else {\n                head = text.Left(i + 1);\n            }\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            TextExtent he = MeasureText(head);\n            i32 headW = he.width;\n            text = text.Right(len - i - 1);\n"
    },
    {
     "name": "scan_len=fresh-nl_len=fresh-head_extent=direct-right_count=fresh",
     "replace": "            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < text.GetLength()) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < text.GetLength() && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head;\n            if (breakNL) {\n                head = text.Left(i);\n            } else {\n                head = text.Left(i + 1);\n            }\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            i32 headW = MeasureText(head).width;\n            text = text.Right(text.GetLength() - i - 1);\n"
    },
    {
     "name": "scan_len=fresh-nl_len=fresh-head_extent=direct-right_count=outerlen",
     "replace": "            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < text.GetLength()) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < text.GetLength() && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head;\n            if (breakNL) {\n                head = text.Left(i);\n            } else {\n                head = text.Left(i + 1);\n            }\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            i32 headW = MeasureText(head).width;\n            text = text.Right(len - i - 1);\n"
    },
    {
     "name": "scan_len=fresh-nl_len=outerlen-head_extent=copy-right_count=fresh",
     "replace": "            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < text.GetLength()) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < len && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head;\n            if (breakNL) {\n                head = text.Left(i);\n            } else {\n                head = text.Left(i + 1);\n            }\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            TextExtent he = MeasureText(head);\n            i32 headW = he.width;\n            text = text.Right(text.GetLength() - i - 1);\n"
    },
    {
     "name": "scan_len=fresh-nl_len=outerlen-head_extent=copy-right_count=outerlen",
     "replace": "            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < text.GetLength()) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < len && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head;\n            if (breakNL) {\n                head = text.Left(i);\n            } else {\n                head = text.Left(i + 1);\n            }\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            TextExtent he = MeasureText(head);\n            i32 headW = he.width;\n            text = text.Right(len - i - 1);\n"
    },
    {
     "name": "scan_len=fresh-nl_len=outerlen-head_extent=direct-right_count=fresh",
     "replace": "            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < text.GetLength()) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < len && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head;\n            if (breakNL) {\n                head = text.Left(i);\n            } else {\n                head = text.Left(i + 1);\n            }\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            i32 headW = MeasureText(head).width;\n            text = text.Right(text.GetLength() - i - 1);\n"
    },
    {
     "name": "scan_len=fresh-nl_len=outerlen-head_extent=direct-right_count=outerlen",
     "replace": "            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < text.GetLength()) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < len && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head;\n            if (breakNL) {\n                head = text.Left(i);\n            } else {\n                head = text.Left(i + 1);\n            }\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            i32 headW = MeasureText(head).width;\n            text = text.Right(len - i - 1);\n"
    },
    {
     "name": "scan_len=outerlen-nl_len=fresh-head_extent=copy-right_count=fresh",
     "replace": "            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < len) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < text.GetLength() && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head;\n            if (breakNL) {\n                head = text.Left(i);\n            } else {\n                head = text.Left(i + 1);\n            }\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            TextExtent he = MeasureText(head);\n            i32 headW = he.width;\n            text = text.Right(text.GetLength() - i - 1);\n"
    },
    {
     "name": "scan_len=outerlen-nl_len=fresh-head_extent=copy-right_count=outerlen",
     "replace": "            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < len) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < text.GetLength() && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head;\n            if (breakNL) {\n                head = text.Left(i);\n            } else {\n                head = text.Left(i + 1);\n            }\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            TextExtent he = MeasureText(head);\n            i32 headW = he.width;\n            text = text.Right(len - i - 1);\n"
    },
    {
     "name": "scan_len=outerlen-nl_len=fresh-head_extent=direct-right_count=fresh",
     "replace": "            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < len) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < text.GetLength() && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head;\n            if (breakNL) {\n                head = text.Left(i);\n            } else {\n                head = text.Left(i + 1);\n            }\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            i32 headW = MeasureText(head).width;\n            text = text.Right(text.GetLength() - i - 1);\n"
    },
    {
     "name": "scan_len=outerlen-nl_len=fresh-head_extent=direct-right_count=outerlen",
     "replace": "            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < len) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < text.GetLength() && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head;\n            if (breakNL) {\n                head = text.Left(i);\n            } else {\n                head = text.Left(i + 1);\n            }\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            i32 headW = MeasureText(head).width;\n            text = text.Right(len - i - 1);\n"
    },
    {
     "name": "scan_len=outerlen-nl_len=outerlen-head_extent=copy-right_count=fresh",
     "replace": "            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < len) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < len && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head;\n            if (breakNL) {\n                head = text.Left(i);\n            } else {\n                head = text.Left(i + 1);\n            }\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            TextExtent he = MeasureText(head);\n            i32 headW = he.width;\n            text = text.Right(text.GetLength() - i - 1);\n"
    },
    {
     "name": "scan_len=outerlen-nl_len=outerlen-head_extent=copy-right_count=outerlen",
     "replace": "            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < len) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < len && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head;\n            if (breakNL) {\n                head = text.Left(i);\n            } else {\n                head = text.Left(i + 1);\n            }\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            TextExtent he = MeasureText(head);\n            i32 headW = he.width;\n            text = text.Right(len - i - 1);\n"
    },
    {
     "name": "scan_len=outerlen-nl_len=outerlen-head_extent=direct-right_count=fresh",
     "replace": "            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < len) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < len && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head;\n            if (breakNL) {\n                head = text.Left(i);\n            } else {\n                head = text.Left(i + 1);\n            }\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            i32 headW = MeasureText(head).width;\n            text = text.Right(text.GetLength() - i - 1);\n"
    },
    {
     "name": "scan_len=outerlen-nl_len=outerlen-head_extent=direct-right_count=outerlen",
     "replace": "            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < len) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < len && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head;\n            if (breakNL) {\n                head = text.Left(i);\n            } else {\n                head = text.Left(i + 1);\n            }\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            i32 headW = MeasureText(head).width;\n            text = text.Right(len - i - 1);\n"
    }
   ]
  },
  {
   "name": "char_extent",
   "find": "                        i32 chW = MeasureText(CString(head.GetAt(0), 1)).width;\n                        if (chW + x > rc.right) {\n",
   "options": [
    {
     "name": "direct"
    },
    {
     "name": "copy",
     "replace": "                        TextExtent ce = MeasureText(CString(head.GetAt(0), 1));\n                        i32 chW = ce.width;\n                        if (chW + x > rc.right) {\n"
    }
   ]
  }
 ]
}