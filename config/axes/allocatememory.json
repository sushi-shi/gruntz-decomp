{
 "schema": 1,
 "source": "src/Font/Font.cpp",
 "rva": "0x179720",
 "axes": [
  {
   "name": "alloc",
   "find": "    m_surfaces = static_cast<void**>(operator new(m_count * sizeof(void*)));\n    m_glyphs = new Glyph[m_count];\n",
   "options": [
    {
     "name": "base"
    },
    {
     "name": "count_local",
     "replace": "    i32 n = m_count;\n    m_surfaces = static_cast<void**>(operator new(n * sizeof(void*)));\n    m_glyphs = new Glyph[n];\n"
    }
   ]
  },
  {
   "name": "zeroloop",
   "find": "    for (i32 i = 0; i < m_count; i++) {\n        m_surfaces[i] = 0;\n        m_glyphs[i].width = 0;\n        m_glyphs[i].height = 0;\n    }\n",
   "options": [
    {
     "name": "base"
    },
    {
     "name": "glyph_ref",
     "replace": "    for (i32 i = 0; i < m_count; i++) {\n        m_surfaces[i] = 0;\n        Glyph& g = m_glyphs[i];\n        g.width = 0;\n        g.height = 0;\n    }\n"
    },
    {
     "name": "glyph_ptr",
     "replace": "    for (i32 i = 0; i < m_count; i++) {\n        m_surfaces[i] = 0;\n        Glyph* g = m_glyphs + i;\n        g->width = 0;\n        g->height = 0;\n    }\n"
    },
    {
     "name": "struct_copy",
     "replace": "    for (i32 i = 0; i < m_count; i++) {\n        m_surfaces[i] = 0;\n        Glyph g;\n        g.width = 0;\n        g.height = 0;\n        m_glyphs[i] = g;\n    }\n"
    },
    {
     "name": "height_first",
     "replace": "    for (i32 i = 0; i < m_count; i++) {\n        m_surfaces[i] = 0;\n        m_glyphs[i].height = 0;\n        m_glyphs[i].width = 0;\n    }\n"
    },
    {
     "name": "surfaces_last",
     "replace": "    for (i32 i = 0; i < m_count; i++) {\n        m_glyphs[i].width = 0;\n        m_glyphs[i].height = 0;\n        m_surfaces[i] = 0;\n    }\n"
    }
   ]
  }
 ]
}