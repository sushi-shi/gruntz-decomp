{
 "schema": 1,
 "source": "src/Gruntz/GruntzMgr.cpp",
 "rva": "0x8f530",
 "axes": [
  {
   "name": "deadline_form",
   "find": "    i32 base;\n    if (out != 0) {\n        out = 0; // retail re-zeroes the out slot before the second lookup (mov [esp+0x14],0)\n        MapLookup(m_world->m_soundRegistry->m_10, \"MENU_ACTIVATE\", out);\n        base = out->m_10->m_durationMs + 0x1f4; // cue duration + 500ms: wait out the cue\n    } else {\n        base = 0;\n    }\n    base += ::timeGetTime();\n    u32 deadline = base;\n    while (::timeGetTime() < deadline) {\n    }",
   "options": [
    {
     "name": "baseline"
    },
    {
     "name": "u32_base_no_deadline",
     "replace": "    u32 base;\n    if (out != 0) {\n        out = 0; // retail re-zeroes the out slot before the second lookup\n        MapLookup(m_world->m_soundRegistry->m_10, \"MENU_ACTIVATE\", out);\n        base = out->m_10->m_durationMs + 0x1f4;\n    } else {\n        base = 0;\n    }\n    base += ::timeGetTime();\n    while (::timeGetTime() < base) {\n    }"
    },
    {
     "name": "deadline_declared_first",
     "replace": "    u32 deadline;\n    if (out != 0) {\n        out = 0; // retail re-zeroes the out slot before the second lookup\n        MapLookup(m_world->m_soundRegistry->m_10, \"MENU_ACTIVATE\", out);\n        deadline = out->m_10->m_durationMs + 0x1f4;\n    } else {\n        deadline = 0;\n    }\n    deadline += ::timeGetTime();\n    while (::timeGetTime() < deadline) {\n    }"
    },
    {
     "name": "sum_expression",
     "replace": "    u32 base;\n    if (out != 0) {\n        out = 0; // retail re-zeroes the out slot before the second lookup\n        MapLookup(m_world->m_soundRegistry->m_10, \"MENU_ACTIVATE\", out);\n        base = out->m_10->m_durationMs + 0x1f4;\n    } else {\n        base = 0;\n    }\n    u32 deadline = ::timeGetTime() + base;\n    while (::timeGetTime() < deadline) {\n    }"
    },
    {
     "name": "for_break",
     "replace": "    u32 base;\n    if (out != 0) {\n        out = 0; // retail re-zeroes the out slot before the second lookup\n        MapLookup(m_world->m_soundRegistry->m_10, \"MENU_ACTIVATE\", out);\n        base = out->m_10->m_durationMs + 0x1f4;\n    } else {\n        base = 0;\n    }\n    base += ::timeGetTime();\n    for (;;) {\n        if (::timeGetTime() >= base) {\n            break;\n        }\n    }"
    },
    {
     "name": "base_init_zero_no_else",
     "replace": "    u32 base = 0;\n    if (out != 0) {\n        out = 0; // retail re-zeroes the out slot before the second lookup\n        MapLookup(m_world->m_soundRegistry->m_10, \"MENU_ACTIVATE\", out);\n        base = out->m_10->m_durationMs + 0x1f4;\n    }\n    base += ::timeGetTime();\n    while (::timeGetTime() < base) {\n    }"
    },
    {
     "name": "empty_stmt_loop",
     "replace": "    u32 base;\n    if (out != 0) {\n        out = 0; // retail re-zeroes the out slot before the second lookup\n        MapLookup(m_world->m_soundRegistry->m_10, \"MENU_ACTIVATE\", out);\n        base = out->m_10->m_durationMs + 0x1f4;\n    } else {\n        base = 0;\n    }\n    base += ::timeGetTime();\n    while (::timeGetTime() < base)\n        ;"
    },
    {
     "name": "cue_local",
     "replace": "    u32 base;\n    if (out != 0) {\n        out = 0; // retail re-zeroes the out slot before the second lookup\n        MapLookup(m_world->m_soundRegistry->m_10, \"MENU_ACTIVATE\", out);\n        DSoundCloneInst* st = out->m_10;\n        base = st->m_durationMs + 0x1f4;\n    } else {\n        base = 0;\n    }\n    base += ::timeGetTime();\n    while (::timeGetTime() < base) {\n    }"
    }
   ]
  },
  {
   "name": "tail_form",
   "find": "    if (m_owner) {\n        m_owner->m_running = 0;\n    }\n    if (m_gameWnd) {\n        ::PostMessageA(m_gameWnd->m_hwnd, 0x10, 0, 0);\n    }\n}",
   "options": [
    {
     "name": "baseline"
    },
    {
     "name": "ne_zero",
     "replace": "    if (m_owner != 0) {\n        m_owner->m_running = 0;\n    }\n    if (m_gameWnd != 0) {\n        ::PostMessageA(m_gameWnd->m_hwnd, 0x10, 0, 0);\n    }\n}"
    }
   ]
  }
 ]
}