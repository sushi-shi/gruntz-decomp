{
 "schema": 1,
 "source": "src/Gruntz/TriggerMgrGrid.cpp",
 "rva": "0x6dae0",
 "axes": [
  {
   "name": "tile_locals",
   "find": "    i32 cellTileX = cell->m_lastTilePxX >> 5;\n    i32 cellTileY = cell->m_lastTilePxY >> 5;\n    i32 argTileX = worldX >> 5;\n    i32 argTileY = worldY >> 5;\n    CGameObject* o = cell->m_object;\n    if (o->m_screenX != cell->m_lastTilePxX) {\n        return -1;\n    }\n    if (o->m_screenY != cell->m_lastTilePxY) {\n        return -1;\n    }",
   "options": [
    {
     "name": "baseline"
    },
    {
     "name": "raw_locals_dead",
     "replace": "    i32 cellTileX = cell->m_lastTilePxX >> 5;\n    i32 lastY = cell->m_lastTilePxY;\n    i32 lastX = cell->m_lastTilePxX;\n    i32 cellTileY = cell->m_lastTilePxY >> 5;\n    i32 argTileX = worldX >> 5;\n    i32 argTileY = worldY >> 5;\n    CGameObject* o = cell->m_object;\n    if (o->m_screenX != cell->m_lastTilePxX) {\n        return -1;\n    }\n    if (o->m_screenY != cell->m_lastTilePxY) {\n        return -1;\n    }\n    static_cast<void>(lastX);\n    static_cast<void>(lastY);"
    },
    {
     "name": "raw_locals_used",
     "replace": "    i32 lastX = cell->m_lastTilePxX;\n    i32 lastY = cell->m_lastTilePxY;\n    i32 cellTileX = lastX >> 5;\n    i32 cellTileY = lastY >> 5;\n    i32 argTileX = worldX >> 5;\n    i32 argTileY = worldY >> 5;\n    CGameObject* o = cell->m_object;\n    if (o->m_screenX != lastX) {\n        return -1;\n    }\n    if (o->m_screenY != lastY) {\n        return -1;\n    }"
    },
    {
     "name": "tilexy_last",
     "replace": "    i32 cellTileX = cell->m_lastTilePxX >> 5;\n    i32 argTileX = worldX >> 5;\n    i32 cellTileY = cell->m_lastTilePxY >> 5;\n    i32 argTileY = worldY >> 5;\n    CGameObject* o = cell->m_object;\n    if (o->m_screenX != cell->m_lastTilePxX) {\n        return -1;\n    }\n    if (o->m_screenY != cell->m_lastTilePxY) {\n        return -1;\n    }"
    },
    {
     "name": "obj_first",
     "replace": "    CGameObject* o = cell->m_object;\n    i32 cellTileX = cell->m_lastTilePxX >> 5;\n    i32 cellTileY = cell->m_lastTilePxY >> 5;\n    i32 argTileX = worldX >> 5;\n    i32 argTileY = worldY >> 5;\n    if (o->m_screenX != cell->m_lastTilePxX) {\n        return -1;\n    }\n    if (o->m_screenY != cell->m_lastTilePxY) {\n        return -1;\n    }"
    },
    {
     "name": "cellY_then_argY",
     "replace": "    i32 cellTileX = cell->m_lastTilePxX >> 5;\n    i32 cellTileY = cell->m_lastTilePxY >> 5;\n    CGameObject* o = cell->m_object;\n    if (o->m_screenX != cell->m_lastTilePxX) {\n        return -1;\n    }\n    if (o->m_screenY != cell->m_lastTilePxY) {\n        return -1;\n    }\n    i32 argTileX = worldX >> 5;\n    i32 argTileY = worldY >> 5;"
    }
   ]
  }
 ]
}