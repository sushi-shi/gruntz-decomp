{
 "schema": 1,
 "source": "src/Font/Font.cpp",
 "rva": "0x17ad10",
 "axes": [
  {
   "name": "prefix",
   "find": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        TextExtent e = MeasureText(text);\n        if (e.width + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < text.GetLength()) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < text.GetLength() && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            TextExtent he = MeasureText(head);\n            i32 headW = he.width;\n            text = text.Right(text.GetLength() - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n",
   "options": [
    {
     "name": "first_extent=copy-scan_len=fresh-nl_len=fresh-head_extent=copy-right_count=fresh"
    },
    {
     "name": "first_extent=copy-scan_len=fresh-nl_len=fresh-head_extent=copy-right_count=outerlen",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        TextExtent e = MeasureText(text);\n        if (e.width + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < text.GetLength()) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < text.GetLength() && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            TextExtent he = MeasureText(head);\n            i32 headW = he.width;\n            text = text.Right(len - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=copy-scan_len=fresh-nl_len=fresh-head_extent=direct-right_count=fresh",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        TextExtent e = MeasureText(text);\n        if (e.width + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < text.GetLength()) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < text.GetLength() && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            i32 headW = MeasureText(head).width;\n            text = text.Right(text.GetLength() - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=copy-scan_len=fresh-nl_len=fresh-head_extent=direct-right_count=outerlen",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        TextExtent e = MeasureText(text);\n        if (e.width + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < text.GetLength()) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < text.GetLength() && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            i32 headW = MeasureText(head).width;\n            text = text.Right(len - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=copy-scan_len=fresh-nl_len=outerlen-head_extent=copy-right_count=fresh",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        TextExtent e = MeasureText(text);\n        if (e.width + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < text.GetLength()) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < len && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            TextExtent he = MeasureText(head);\n            i32 headW = he.width;\n            text = text.Right(text.GetLength() - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=copy-scan_len=fresh-nl_len=outerlen-head_extent=copy-right_count=outerlen",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        TextExtent e = MeasureText(text);\n        if (e.width + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < text.GetLength()) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < len && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            TextExtent he = MeasureText(head);\n            i32 headW = he.width;\n            text = text.Right(len - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=copy-scan_len=fresh-nl_len=outerlen-head_extent=direct-right_count=fresh",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        TextExtent e = MeasureText(text);\n        if (e.width + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < text.GetLength()) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < len && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            i32 headW = MeasureText(head).width;\n            text = text.Right(text.GetLength() - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=copy-scan_len=fresh-nl_len=outerlen-head_extent=direct-right_count=outerlen",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        TextExtent e = MeasureText(text);\n        if (e.width + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < text.GetLength()) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < len && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            i32 headW = MeasureText(head).width;\n            text = text.Right(len - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=copy-scan_len=outerlen-nl_len=fresh-head_extent=copy-right_count=fresh",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        TextExtent e = MeasureText(text);\n        if (e.width + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < len) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < text.GetLength() && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            TextExtent he = MeasureText(head);\n            i32 headW = he.width;\n            text = text.Right(text.GetLength() - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=copy-scan_len=outerlen-nl_len=fresh-head_extent=copy-right_count=outerlen",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        TextExtent e = MeasureText(text);\n        if (e.width + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < len) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < text.GetLength() && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            TextExtent he = MeasureText(head);\n            i32 headW = he.width;\n            text = text.Right(len - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=copy-scan_len=outerlen-nl_len=fresh-head_extent=direct-right_count=fresh",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        TextExtent e = MeasureText(text);\n        if (e.width + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < len) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < text.GetLength() && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            i32 headW = MeasureText(head).width;\n            text = text.Right(text.GetLength() - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=copy-scan_len=outerlen-nl_len=fresh-head_extent=direct-right_count=outerlen",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        TextExtent e = MeasureText(text);\n        if (e.width + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < len) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < text.GetLength() && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            i32 headW = MeasureText(head).width;\n            text = text.Right(len - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=copy-scan_len=outerlen-nl_len=outerlen-head_extent=copy-right_count=fresh",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        TextExtent e = MeasureText(text);\n        if (e.width + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < len) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < len && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            TextExtent he = MeasureText(head);\n            i32 headW = he.width;\n            text = text.Right(text.GetLength() - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=copy-scan_len=outerlen-nl_len=outerlen-head_extent=copy-right_count=outerlen",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        TextExtent e = MeasureText(text);\n        if (e.width + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < len) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < len && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            TextExtent he = MeasureText(head);\n            i32 headW = he.width;\n            text = text.Right(len - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=copy-scan_len=outerlen-nl_len=outerlen-head_extent=direct-right_count=fresh",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        TextExtent e = MeasureText(text);\n        if (e.width + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < len) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < len && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            i32 headW = MeasureText(head).width;\n            text = text.Right(text.GetLength() - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=copy-scan_len=outerlen-nl_len=outerlen-head_extent=direct-right_count=outerlen",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        TextExtent e = MeasureText(text);\n        if (e.width + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < len) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < len && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            i32 headW = MeasureText(head).width;\n            text = text.Right(len - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=split-scan_len=fresh-nl_len=fresh-head_extent=copy-right_count=fresh",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        TextExtent e = MeasureText(text);\n        i32 eW = e.width;\n        if (eW + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < text.GetLength()) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < text.GetLength() && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            TextExtent he = MeasureText(head);\n            i32 headW = he.width;\n            text = text.Right(text.GetLength() - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=split-scan_len=fresh-nl_len=fresh-head_extent=copy-right_count=outerlen",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        TextExtent e = MeasureText(text);\n        i32 eW = e.width;\n        if (eW + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < text.GetLength()) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < text.GetLength() && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            TextExtent he = MeasureText(head);\n            i32 headW = he.width;\n            text = text.Right(len - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=split-scan_len=fresh-nl_len=fresh-head_extent=direct-right_count=fresh",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        TextExtent e = MeasureText(text);\n        i32 eW = e.width;\n        if (eW + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < text.GetLength()) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < text.GetLength() && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            i32 headW = MeasureText(head).width;\n            text = text.Right(text.GetLength() - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=split-scan_len=fresh-nl_len=fresh-head_extent=direct-right_count=outerlen",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        TextExtent e = MeasureText(text);\n        i32 eW = e.width;\n        if (eW + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < text.GetLength()) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < text.GetLength() && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            i32 headW = MeasureText(head).width;\n            text = text.Right(len - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=split-scan_len=fresh-nl_len=outerlen-head_extent=copy-right_count=fresh",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        TextExtent e = MeasureText(text);\n        i32 eW = e.width;\n        if (eW + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < text.GetLength()) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < len && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            TextExtent he = MeasureText(head);\n            i32 headW = he.width;\n            text = text.Right(text.GetLength() - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=split-scan_len=fresh-nl_len=outerlen-head_extent=copy-right_count=outerlen",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        TextExtent e = MeasureText(text);\n        i32 eW = e.width;\n        if (eW + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < text.GetLength()) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < len && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            TextExtent he = MeasureText(head);\n            i32 headW = he.width;\n            text = text.Right(len - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=split-scan_len=fresh-nl_len=outerlen-head_extent=direct-right_count=fresh",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        TextExtent e = MeasureText(text);\n        i32 eW = e.width;\n        if (eW + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < text.GetLength()) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < len && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            i32 headW = MeasureText(head).width;\n            text = text.Right(text.GetLength() - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=split-scan_len=fresh-nl_len=outerlen-head_extent=direct-right_count=outerlen",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        TextExtent e = MeasureText(text);\n        i32 eW = e.width;\n        if (eW + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < text.GetLength()) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < len && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            i32 headW = MeasureText(head).width;\n            text = text.Right(len - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=split-scan_len=outerlen-nl_len=fresh-head_extent=copy-right_count=fresh",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        TextExtent e = MeasureText(text);\n        i32 eW = e.width;\n        if (eW + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < len) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < text.GetLength() && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            TextExtent he = MeasureText(head);\n            i32 headW = he.width;\n            text = text.Right(text.GetLength() - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=split-scan_len=outerlen-nl_len=fresh-head_extent=copy-right_count=outerlen",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        TextExtent e = MeasureText(text);\n        i32 eW = e.width;\n        if (eW + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < len) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < text.GetLength() && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            TextExtent he = MeasureText(head);\n            i32 headW = he.width;\n            text = text.Right(len - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=split-scan_len=outerlen-nl_len=fresh-head_extent=direct-right_count=fresh",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        TextExtent e = MeasureText(text);\n        i32 eW = e.width;\n        if (eW + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < len) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < text.GetLength() && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            i32 headW = MeasureText(head).width;\n            text = text.Right(text.GetLength() - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=split-scan_len=outerlen-nl_len=fresh-head_extent=direct-right_count=outerlen",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        TextExtent e = MeasureText(text);\n        i32 eW = e.width;\n        if (eW + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < len) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < text.GetLength() && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            i32 headW = MeasureText(head).width;\n            text = text.Right(len - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=split-scan_len=outerlen-nl_len=outerlen-head_extent=copy-right_count=fresh",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        TextExtent e = MeasureText(text);\n        i32 eW = e.width;\n        if (eW + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < len) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < len && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            TextExtent he = MeasureText(head);\n            i32 headW = he.width;\n            text = text.Right(text.GetLength() - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=split-scan_len=outerlen-nl_len=outerlen-head_extent=copy-right_count=outerlen",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        TextExtent e = MeasureText(text);\n        i32 eW = e.width;\n        if (eW + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < len) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < len && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            TextExtent he = MeasureText(head);\n            i32 headW = he.width;\n            text = text.Right(len - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=split-scan_len=outerlen-nl_len=outerlen-head_extent=direct-right_count=fresh",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        TextExtent e = MeasureText(text);\n        i32 eW = e.width;\n        if (eW + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < len) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < len && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            i32 headW = MeasureText(head).width;\n            text = text.Right(text.GetLength() - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=split-scan_len=outerlen-nl_len=outerlen-head_extent=direct-right_count=outerlen",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        TextExtent e = MeasureText(text);\n        i32 eW = e.width;\n        if (eW + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < len) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < len && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            i32 headW = MeasureText(head).width;\n            text = text.Right(len - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=direct-scan_len=fresh-nl_len=fresh-head_extent=copy-right_count=fresh",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        if (MeasureText(text).width + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < text.GetLength()) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < text.GetLength() && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            TextExtent he = MeasureText(head);\n            i32 headW = he.width;\n            text = text.Right(text.GetLength() - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=direct-scan_len=fresh-nl_len=fresh-head_extent=copy-right_count=outerlen",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        if (MeasureText(text).width + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < text.GetLength()) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < text.GetLength() && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            TextExtent he = MeasureText(head);\n            i32 headW = he.width;\n            text = text.Right(len - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=direct-scan_len=fresh-nl_len=fresh-head_extent=direct-right_count=fresh",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        if (MeasureText(text).width + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < text.GetLength()) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < text.GetLength() && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            i32 headW = MeasureText(head).width;\n            text = text.Right(text.GetLength() - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=direct-scan_len=fresh-nl_len=fresh-head_extent=direct-right_count=outerlen",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        if (MeasureText(text).width + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < text.GetLength()) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < text.GetLength() && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            i32 headW = MeasureText(head).width;\n            text = text.Right(len - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=direct-scan_len=fresh-nl_len=outerlen-head_extent=copy-right_count=fresh",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        if (MeasureText(text).width + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < text.GetLength()) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < len && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            TextExtent he = MeasureText(head);\n            i32 headW = he.width;\n            text = text.Right(text.GetLength() - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=direct-scan_len=fresh-nl_len=outerlen-head_extent=copy-right_count=outerlen",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        if (MeasureText(text).width + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < text.GetLength()) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < len && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            TextExtent he = MeasureText(head);\n            i32 headW = he.width;\n            text = text.Right(len - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=direct-scan_len=fresh-nl_len=outerlen-head_extent=direct-right_count=fresh",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        if (MeasureText(text).width + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < text.GetLength()) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < len && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            i32 headW = MeasureText(head).width;\n            text = text.Right(text.GetLength() - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=direct-scan_len=fresh-nl_len=outerlen-head_extent=direct-right_count=outerlen",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        if (MeasureText(text).width + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < text.GetLength()) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < len && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            i32 headW = MeasureText(head).width;\n            text = text.Right(len - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=direct-scan_len=outerlen-nl_len=fresh-head_extent=copy-right_count=fresh",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        if (MeasureText(text).width + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < len) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < text.GetLength() && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            TextExtent he = MeasureText(head);\n            i32 headW = he.width;\n            text = text.Right(text.GetLength() - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=direct-scan_len=outerlen-nl_len=fresh-head_extent=copy-right_count=outerlen",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        if (MeasureText(text).width + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < len) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < text.GetLength() && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            TextExtent he = MeasureText(head);\n            i32 headW = he.width;\n            text = text.Right(len - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=direct-scan_len=outerlen-nl_len=fresh-head_extent=direct-right_count=fresh",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        if (MeasureText(text).width + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < len) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < text.GetLength() && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            i32 headW = MeasureText(head).width;\n            text = text.Right(text.GetLength() - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=direct-scan_len=outerlen-nl_len=fresh-head_extent=direct-right_count=outerlen",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        if (MeasureText(text).width + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < len) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < text.GetLength() && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            i32 headW = MeasureText(head).width;\n            text = text.Right(len - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=direct-scan_len=outerlen-nl_len=outerlen-head_extent=copy-right_count=fresh",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        if (MeasureText(text).width + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < len) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < len && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            TextExtent he = MeasureText(head);\n            i32 headW = he.width;\n            text = text.Right(text.GetLength() - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=direct-scan_len=outerlen-nl_len=outerlen-head_extent=copy-right_count=outerlen",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        if (MeasureText(text).width + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < len) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < len && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            TextExtent he = MeasureText(head);\n            i32 headW = he.width;\n            text = text.Right(len - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=direct-scan_len=outerlen-nl_len=outerlen-head_extent=direct-right_count=fresh",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        if (MeasureText(text).width + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < len) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < len && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            i32 headW = MeasureText(head).width;\n            text = text.Right(text.GetLength() - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    },
    {
     "name": "first_extent=direct-scan_len=outerlen-nl_len=outerlen-head_extent=direct-right_count=outerlen",
     "replace": "TextExtent FontRenderer::MeasureWrapped(CString text, i32 x0, i32 top, i32 right, i32 bottom) {\n    TextExtent ext;\n    i32 maxWidth = 0;\n    i32 y = top;\n    i32 x = x0;\n\n    CString line;\n    while (y < bottom) {\n        i32 len = text.GetLength();\n        if (len <= 0) {\n            break;\n        }\n\n        i32 nl = 0;\n        for (i32 k = 0; k < len; k++) {\n            if (text[k] == '\\n') {\n                nl = 1;\n                break;\n            }\n        }\n\n        if (MeasureText(text).width + x <= right && !nl) {\n            line += text;\n            text = \"\";\n            if (m_font->GetMaxHeight() + y <= bottom) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n            }\n        } else {\n            i32 i = 0;\n            i32 breakNL = 0;\n            // text.GetLength() again, not the outer `len`: retail re-loads it here\n            // (0x17ae3a `mov edx,[ecx-8]`) and CSEs it with the newline test below,\n            // which is why the entry guard folds straight past that test.\n            while (i < len) {\n                u8 ch = text[i];\n                if (ch == ' ' || ch == '\\n') {\n                    break;\n                }\n                i++;\n            }\n            if (i < len && text[i] == '\\n') {\n                breakNL = 1;\n            }\n            CString head = text.Left(i + 1);\n            // `he` is a real named copy of the extent - retail stores the height it\n            // never reads (0x17aea0 `mov ecx,[eax+4]` / `mov [esp+0x28],ecx`), which\n            // only a whole-struct copy produces; and the Right() count re-reads\n            // text.GetLength() (0x17aea3 `mov eax,[edx-8]`), not the outer `len`.\n            i32 headW = MeasureText(head).width;\n            text = text.Right(len - i - 1);\n            if (headW + x < right) {\n                line += head;\n                x = headW + x;\n            } else if (headW < right - x0) {\n                i32 w = MeasureText(line).width;\n                if (maxWidth <= w) {\n                    maxWidth = w;\n                }\n                y = y + m_font->GetMaxHeight();\n                x = x0;\n                line = \"\";\n                if (m_font->GetMaxHeight() + y < bottom) {\n                    line += head;\n                    x = headW + x0;\n                }\n            } else {\n"
    }
   ]
  },
  {
   "name": "char_guard",
   "find": "                i32 j = 0;\n                if (head.GetLength() > 0) {\n                    while (y < bottom) {\n",
   "options": [
    {
     "name": "j_first"
    },
    {
     "name": "len_var",
     "replace": "                i32 headLen = head.GetLength();\n                if (headLen > 0) {\n                    i32 j = 0;\n                    while (y < bottom) {\n"
    }
   ]
  },
  {
   "name": "char_extent",
   "find": "                        i32 chW = MeasureText(CString(head.GetAt(j), 1)).width;\n",
   "options": [
    {
     "name": "direct"
    },
    {
     "name": "copy",
     "replace": "                        TextExtent ce = MeasureText(CString(head.GetAt(j), 1));\n                        i32 chW = ce.width;\n"
    }
   ]
  }
 ]
}