{
 "schema": 1,
 "source": "src/Gruntz/GruntArrivalScan.cpp",
 "rva": "0xec670",
 "axes": [
  {
   "name": "origin_order",
   "find": "                i32 spanX = abs(h->m_extent.right - h->m_extent.left);\n                i32 spanY = abs(h->m_extent.bottom - h->m_extent.top);\n                i32 outX = h->m_extent.left;\n                i32 outY = h->m_extent.top;",
   "options": [
    {
     "name": "baseline"
    },
    {
     "name": "outY_before_outX",
     "replace": "                i32 spanX = abs(h->m_extent.right - h->m_extent.left);\n                i32 spanY = abs(h->m_extent.bottom - h->m_extent.top);\n                i32 outY = h->m_extent.top;\n                i32 outX = h->m_extent.left;"
    },
    {
     "name": "spanY_first",
     "replace": "                i32 spanY = abs(h->m_extent.bottom - h->m_extent.top);\n                i32 spanX = abs(h->m_extent.right - h->m_extent.left);\n                i32 outX = h->m_extent.left;\n                i32 outY = h->m_extent.top;"
    },
    {
     "name": "spanY_first_outY_first",
     "replace": "                i32 spanY = abs(h->m_extent.bottom - h->m_extent.top);\n                i32 spanX = abs(h->m_extent.right - h->m_extent.left);\n                i32 outY = h->m_extent.top;\n                i32 outX = h->m_extent.left;"
    },
    {
     "name": "interleave_spanX_outX",
     "replace": "                i32 spanX = abs(h->m_extent.right - h->m_extent.left);\n                i32 outX = h->m_extent.left;\n                i32 spanY = abs(h->m_extent.bottom - h->m_extent.top);\n                i32 outY = h->m_extent.top;"
    }
   ]
  }
 ]
}
