{
 "schema": 1,
 "source": "src/Dsndmgr/SoundStream.cpp",
 "rva": "0x137b70",
 "axes": [
  {
   "name": "header_decls",
   "find": "    i32 gotFmt = 0;\n    i32 gotData = 0;\n    src->SetPos(0);\n\n    u32 riffTag;\n    u32 riffSize;\n    u32 waveTag;\n    src->Read(&riffTag, 4, -1);\n    src->Read(&riffSize, 4, -1);\n    src->Read(&waveTag, 4, -1);\n    if (riffTag != 0x46464952) {\n        return 0;\n    }\n    if (waveTag != 0x45564157) {\n        return 0;\n    }\n",
   "options": [
    {
     "name": "order=riffTag-riffSize-waveTag"
    },
    {
     "name": "order=riffTag-waveTag-riffSize",
     "replace": "    i32 gotFmt = 0;\n    i32 gotData = 0;\n    src->SetPos(0);\n\n    u32 riffTag;\n    u32 waveTag;\n    u32 riffSize;\n    src->Read(&riffTag, 4, -1);\n    src->Read(&riffSize, 4, -1);\n    src->Read(&waveTag, 4, -1);\n    if (riffTag != 0x46464952) {\n        return 0;\n    }\n    if (waveTag != 0x45564157) {\n        return 0;\n    }\n"
    },
    {
     "name": "order=riffSize-riffTag-waveTag",
     "replace": "    i32 gotFmt = 0;\n    i32 gotData = 0;\n    src->SetPos(0);\n\n    u32 riffSize;\n    u32 riffTag;\n    u32 waveTag;\n    src->Read(&riffTag, 4, -1);\n    src->Read(&riffSize, 4, -1);\n    src->Read(&waveTag, 4, -1);\n    if (riffTag != 0x46464952) {\n        return 0;\n    }\n    if (waveTag != 0x45564157) {\n        return 0;\n    }\n"
    },
    {
     "name": "order=riffSize-waveTag-riffTag",
     "replace": "    i32 gotFmt = 0;\n    i32 gotData = 0;\n    src->SetPos(0);\n\n    u32 riffSize;\n    u32 waveTag;\n    u32 riffTag;\n    src->Read(&riffTag, 4, -1);\n    src->Read(&riffSize, 4, -1);\n    src->Read(&waveTag, 4, -1);\n    if (riffTag != 0x46464952) {\n        return 0;\n    }\n    if (waveTag != 0x45564157) {\n        return 0;\n    }\n"
    },
    {
     "name": "order=waveTag-riffTag-riffSize",
     "replace": "    i32 gotFmt = 0;\n    i32 gotData = 0;\n    src->SetPos(0);\n\n    u32 waveTag;\n    u32 riffTag;\n    u32 riffSize;\n    src->Read(&riffTag, 4, -1);\n    src->Read(&riffSize, 4, -1);\n    src->Read(&waveTag, 4, -1);\n    if (riffTag != 0x46464952) {\n        return 0;\n    }\n    if (waveTag != 0x45564157) {\n        return 0;\n    }\n"
    },
    {
     "name": "order=waveTag-riffSize-riffTag",
     "replace": "    i32 gotFmt = 0;\n    i32 gotData = 0;\n    src->SetPos(0);\n\n    u32 waveTag;\n    u32 riffSize;\n    u32 riffTag;\n    src->Read(&riffTag, 4, -1);\n    src->Read(&riffSize, 4, -1);\n    src->Read(&waveTag, 4, -1);\n    if (riffTag != 0x46464952) {\n        return 0;\n    }\n    if (waveTag != 0x45564157) {\n        return 0;\n    }\n"
    },
    {
     "name": "one_decl",
     "replace": "    i32 gotFmt = 0;\n    i32 gotData = 0;\n    src->SetPos(0);\n\n    u32 riffTag, riffSize, waveTag;\n    src->Read(&riffTag, 4, -1);\n    src->Read(&riffSize, 4, -1);\n    src->Read(&waveTag, 4, -1);\n    if (riffTag != 0x46464952) {\n        return 0;\n    }\n    if (waveTag != 0x45564157) {\n        return 0;\n    }\n"
    },
    {
     "name": "gots_here",
     "replace": "    src->SetPos(0);\n    i32 gotFmt = 0;\n    i32 gotData = 0;\n\n    u32 riffTag;\n    u32 riffSize;\n    u32 waveTag;\n    src->Read(&riffTag, 4, -1);\n    src->Read(&riffSize, 4, -1);\n    src->Read(&waveTag, 4, -1);\n    if (riffTag != 0x46464952) {\n        return 0;\n    }\n    if (waveTag != 0x45564157) {\n        return 0;\n    }\n"
    }
   ]
  },
  {
   "name": "end_decl",
   "find": "    u32 end = src->m_cursor + riffSize - 4;\n    if (end > src->m_length) {\n        end = src->m_length;\n    }\n",
   "options": [
    {
     "name": "init"
    },
    {
     "name": "split",
     "replace": "    u32 end;\n    end = src->m_cursor + riffSize - 4;\n    if (end > src->m_length) {\n        end = src->m_length;\n    }\n"
    },
    {
     "name": "minmax",
     "replace": "    u32 end = src->m_length;\n    if (src->m_cursor + riffSize - 4 <= end) {\n        end = src->m_cursor + riffSize - 4;\n    }\n"
    }
   ]
  },
  {
   "name": "chunk_decls",
   "find": "        u32 chunkId;\n        u32 chunkSize;\n",
   "options": [
    {
     "name": "in_loop"
    },
    {
     "name": "swapped",
     "replace": "        u32 chunkSize;\n        u32 chunkId;\n"
    },
    {
     "name": "one_decl",
     "replace": "        u32 chunkId, chunkSize;\n"
    }
   ]
  }
 ]
}