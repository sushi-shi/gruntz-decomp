{
 "schema": 1,
 "source": "src/Dsndmgr/SoundStream.cpp",
 "rva": "0x137780",
 "axes": [
  {
   "name": "desc_init2",
   "find": "    wf.cbSize = fmt->cbSize;\n\n    out = 0;\n    desc.dwSize = 0x14;\n    desc.dwFlags = dsFlags;\n    desc.dwBufferBytes = bytes;\n    desc.dwReserved = 0;\n    WaveFormatPtr fmtPtr;\n    fmtPtr.m_rec = &wf;\n    desc.lpwfxFormat = fmtPtr.m_sdk;\n",
   "options": [
    {
     "name": "baseline"
    },
    {
     "name": "memset_only",
     "replace": "    wf.cbSize = fmt->cbSize;\n\n    out = 0;\n    memset(&desc, 0, sizeof(DSBUFFERDESC));\n    desc.dwSize = 0x14;\n    desc.dwFlags = dsFlags;\n    desc.dwBufferBytes = bytes;\n    WaveFormatPtr fmtPtr;\n    fmtPtr.m_rec = &wf;\n    desc.lpwfxFormat = fmtPtr.m_sdk;\n"
    },
    {
     "name": "memset_cbtail",
     "replace": "    wf.cbSize = fmt->cbSize;\n\n    out = 0;\n    memset(&desc, 0, sizeof(DSBUFFERDESC));\n    desc.dwSize = 0x14;\n    desc.dwFlags = dsFlags;\n    desc.dwBufferBytes = bytes;\n    WaveFormatPtr fmtPtr;\n    fmtPtr.m_rec = &wf;\n    desc.lpwfxFormat = fmtPtr.m_sdk;\n    wf.cbSize = 0;\n"
    },
    {
     "name": "explicit5_cbtail",
     "replace": "    wf.cbSize = fmt->cbSize;\n\n    out = 0;\n    desc.dwSize = 0;\n    desc.dwFlags = 0;\n    desc.dwBufferBytes = 0;\n    desc.dwReserved = 0;\n    desc.lpwfxFormat = 0;\n    desc.dwSize = 0x14;\n    desc.dwFlags = dsFlags;\n    desc.dwBufferBytes = bytes;\n    WaveFormatPtr fmtPtr;\n    fmtPtr.m_rec = &wf;\n    desc.lpwfxFormat = fmtPtr.m_sdk;\n    wf.cbSize = 0;\n"
    },
    {
     "name": "memset_first_cbtail",
     "replace": "    wf.cbSize = fmt->cbSize;\n\n    memset(&desc, 0, sizeof(DSBUFFERDESC));\n    out = 0;\n    desc.dwSize = 0x14;\n    desc.dwFlags = dsFlags;\n    desc.dwBufferBytes = bytes;\n    WaveFormatPtr fmtPtr;\n    fmtPtr.m_rec = &wf;\n    desc.lpwfxFormat = fmtPtr.m_sdk;\n    wf.cbSize = 0;\n"
    },
    {
     "name": "explicit5_only",
     "replace": "    wf.cbSize = fmt->cbSize;\n\n    out = 0;\n    desc.dwSize = 0;\n    desc.dwFlags = 0;\n    desc.dwBufferBytes = 0;\n    desc.dwReserved = 0;\n    desc.lpwfxFormat = 0;\n    desc.dwSize = 0x14;\n    desc.dwFlags = dsFlags;\n    desc.dwBufferBytes = bytes;\n    WaveFormatPtr fmtPtr;\n    fmtPtr.m_rec = &wf;\n    desc.lpwfxFormat = fmtPtr.m_sdk;\n"
    }
   ]
  }
 ]
}