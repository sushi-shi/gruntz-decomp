{
 "schema": 1,
 "source": "src/Dsndmgr/GruntzSoundZ.cpp",
 "rva": "0x1389c0",
 "axes": [
  {
   "name": "divide_form",
   "find": "    i32 v = AIL_XMIDI_master_volume(g_ailMidiDriver);\n    if (v <= 0) {\n        return 0;\n    }\n    if (v >= 0x7f) {\n        return 0x64;\n    }\n    return v * 100 / 127;\n}",
   "options": [
    {
     "name": "baseline"
    },
    {
     "name": "named_result",
     "replace": "    i32 v = AIL_XMIDI_master_volume(g_ailMidiDriver);\n    if (v <= 0) {\n        return 0;\n    }\n    if (v >= 0x7f) {\n        return 0x64;\n    }\n    i32 r = v * 100 / 127;\n    return r;\n}"
    },
    {
     "name": "split_mul",
     "replace": "    i32 v = AIL_XMIDI_master_volume(g_ailMidiDriver);\n    if (v <= 0) {\n        return 0;\n    }\n    if (v >= 0x7f) {\n        return 0x64;\n    }\n    i32 scaled = v * 100;\n    return scaled / 127;\n}"
    },
    {
     "name": "named_scaled_and_result",
     "replace": "    i32 v = AIL_XMIDI_master_volume(g_ailMidiDriver);\n    if (v <= 0) {\n        return 0;\n    }\n    if (v >= 0x7f) {\n        return 0x64;\n    }\n    i32 scaled = v * 100;\n    i32 r = scaled / 127;\n    return r;\n}"
    },
    {
     "name": "mul_lhs_const",
     "replace": "    i32 v = AIL_XMIDI_master_volume(g_ailMidiDriver);\n    if (v <= 0) {\n        return 0;\n    }\n    if (v >= 0x7f) {\n        return 0x64;\n    }\n    return 100 * v / 127;\n}"
    },
    {
     "name": "parens",
     "replace": "    i32 v = AIL_XMIDI_master_volume(g_ailMidiDriver);\n    if (v <= 0) {\n        return 0;\n    }\n    if (v >= 0x7f) {\n        return 0x64;\n    }\n    return (v * 100) / 127;\n}"
    },
    {
     "name": "result_declared_top",
     "replace": "    i32 r;\n    i32 v = AIL_XMIDI_master_volume(g_ailMidiDriver);\n    if (v <= 0) {\n        return 0;\n    }\n    if (v >= 0x7f) {\n        return 0x64;\n    }\n    r = v * 100 / 127;\n    return r;\n}"
    },
    {
     "name": "clamp_nested",
     "replace": "    i32 v = AIL_XMIDI_master_volume(g_ailMidiDriver);\n    if (v > 0) {\n        if (v < 0x7f) {\n            return v * 100 / 127;\n        }\n        return 0x64;\n    }\n    return 0;\n}"
    }
   ]
  }
 ]
}