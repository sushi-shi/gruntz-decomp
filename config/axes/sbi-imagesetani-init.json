{
 "schema": 1,
 "source": "src/Gruntz/SBI_ImageSetAni.cpp",
 "rva": "0xe7980",
 "axes": [
  {
   "name": "window_store_block",
   "find": "        m_interval = b2;\n        m_loop = b3;\n        m_step = b4;\n        m_frameStart = (b0 == -1) ? (b4 >= 0 ? tbl->m_minIndex : tbl->m_maxIndex) : b0;\n        m_frameEnd = (b1 == -1) ? (b4 >= 0 ? tbl->m_maxIndex : tbl->m_minIndex) : b1;",
   "options": [
    {
     "name": "baseline"
    },
    {
     "name": "step_local",
     "replace": "        i32 step = b4;\n        m_interval = b2;\n        m_loop = b3;\n        m_step = step;\n        m_frameStart = (b0 == -1) ? (step >= 0 ? tbl->m_minIndex : tbl->m_maxIndex) : b0;\n        m_frameEnd = (b1 == -1) ? (step >= 0 ? tbl->m_maxIndex : tbl->m_minIndex) : b1;"
    },
    {
     "name": "member_reload",
     "replace": "        m_interval = b2;\n        m_loop = b3;\n        m_step = b4;\n        m_frameStart = (b0 == -1) ? (m_step >= 0 ? tbl->m_minIndex : tbl->m_maxIndex) : b0;\n        m_frameEnd = (b1 == -1) ? (m_step >= 0 ? tbl->m_maxIndex : tbl->m_minIndex) : b1;"
    },
    {
     "name": "if_else_arms",
     "replace": "        m_interval = b2;\n        m_loop = b3;\n        m_step = b4;\n        if (b0 == -1) {\n            m_frameStart = (b4 >= 0) ? tbl->m_minIndex : tbl->m_maxIndex;\n        } else {\n            m_frameStart = b0;\n        }\n        if (b1 == -1) {\n            m_frameEnd = (b4 >= 0) ? tbl->m_maxIndex : tbl->m_minIndex;\n        } else {\n            m_frameEnd = b1;\n        }"
    },
    {
     "name": "step_first_load",
     "replace": "        m_step = b4;\n        m_interval = b2;\n        m_loop = b3;\n        m_frameStart = (b0 == -1) ? (b4 >= 0 ? tbl->m_minIndex : tbl->m_maxIndex) : b0;\n        m_frameEnd = (b1 == -1) ? (b4 >= 0 ? tbl->m_maxIndex : tbl->m_minIndex) : b1;"
    }
   ]
  },
  {
   "name": "record_guard",
   "find": "        CObject* found = 0;\n        host->m_imageRegistry->m_10map.Lookup(key, found);\n        CDDrawWorker* tbl = static_cast<CDDrawWorker*>(found);\n        m_34 = tbl;\n        if (tbl == 0) {\n            return 0;\n        }",
   "options": [
    {
     "name": "baseline"
    },
    {
     "name": "reuse_found",
     "replace": "        CObject* found = 0;\n        host->m_imageRegistry->m_10map.Lookup(key, found);\n        m_34 = static_cast<CDDrawWorker*>(found);\n        CDDrawWorker* tbl = m_34;\n        if (tbl == 0) {\n            return 0;\n        }"
    },
    {
     "name": "test_ob_first",
     "replace": "        CObject* found = 0;\n        host->m_imageRegistry->m_10map.Lookup(key, found);\n        CDDrawWorker* tbl = static_cast<CDDrawWorker*>(found);\n        m_34 = tbl;\n        if (found == 0) {\n            return 0;\n        }"
    }
   ]
  }
 ]
}
