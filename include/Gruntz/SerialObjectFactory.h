// SerialObjectFactory.h - the SerialObjectFactory TU's external declarations.
#ifndef GRUNTZ_SERIALOBJECTFACTORY_H
#define GRUNTZ_SERIALOBJECTFACTORY_H

#include <Mfc.h> // afx.h FIRST (umbrella for any Win32 types below)
#include <Ints.h>
#include <rva.h>

i32 __cdecl SerialObjectFactory(void* ctx, void* ar, i32 mode, i32 typeId, void** ppObj);
i32 __cdecl SerialObjectFactory(void* ctx, void* ar, i32 mode, i32 typeId, void** ppObj);

#endif // GRUNTZ_SERIALOBJECTFACTORY_H
