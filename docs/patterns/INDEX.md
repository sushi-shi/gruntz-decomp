# Pattern index (MSVC 5.0 /O2 — Gruntz)

One line per pattern: `- [title](file.md) — cN — tags — symptoms` (cN = confidence /10).
Sorted by primary tag so families cluster. Grep this file by tag (`cpp:switch`,
`cpp:eh`, `topic:wall`) or by symptom token, then read only the hits. Schema +
how-to-search: [`README.md`](README.md). Authoring: new pattern = new file + one
line here, SAME commit.

WALL vs STEERABLE: `topic:codegen-idiom`/`topic:flags` = a source spelling closes the
diff; `topic:wall`/`topic:scoring-artifact` = code already correct / no source form —
stop chasing (orchestration §2a).

- [Redundant `if(flag)` re-test across calls — write SIBLING blocks, not nested](redundant-sibling-guard-retest.md) — c8 — cpp:branch cpp:local | asm:test asm:jcc | topic:codegen-idiom — recompile DROPS a 2nd test reg,reg;je the target keeps, flag pinned in callee-saved esi/edi across calls, ~84%→100% on de-nesting
- [Place an if-body FAR: negate the outer condition (`if(x!=A){if(x==B)}else{..A..}`)](negated-condition-far-block.md) — c8 — cpp:branch | asm:cmp asm:je asm:jne asm:jmp | topic:codegen-idiom — `if(x==A)else if(x==B)` lays bodyA INLINE but retail places it FAR (cmp A;je forward; bodyB; jmp; bodyA); je↔jne on first cmp + the two bodies swapped; negating the outer test reorders the physical blocks, CTriggerMgr::Serialize 88%→100%
- [Unrolled serialize records: `__inline` helper + `switch(mode)`, not `if/else`](inline-switch-serialize-record-unroll.md) — c9 — cpp:inline cpp:switch cpp:method | asm:call asm:cmp asm:je | topic:codegen-idiom — N identical inline Save/Load record blocks; helper-call recompile shows per-field `call`+`add esp,N` (not inlined) or `cmp;jne` if/else polarity; `__inline` + two-case `switch` (mode 4=Write/7=Read) + sparse outer `switch{4,7,8}` subtract-cascade, 38→78→91→100% (CGrunt::SerializeMove 0x53b80)
- [One-shot registrar inlines (unrolled) in some leaf ctor TUs, out-of-line `call` in others](inline-helper-unrolled-vs-out-of-line-call.md) — c8 — cpp:ctor cpp:inline | asm:call asm:jne | topic:codegen-idiom — a folded-base-ctor helper (BuildLogicTypeTable) is `call <helper>` in most leaves but the FULL inlined 3-way Find/Register block in a few; classify by the registrar key-string ref count (compact=1× per key, unrolled=3×); define the helper `inline` in a per-TU header to force the unrolled form (CWayPoint/CGuardPoint/CLevelTime/CLightFx 96%; CDoNothing is compact, revert→90%)
- [Object construction: model a real ctor — spelled-out `new`+stores mis-colors the factory](ctor-vptr-interleave-vs-spelled-out-init.md) — c9 — cpp:ctor cpp:vtable cpp:inline | asm:mov | topic:codegen-idiom topic:regalloc — factory `new T`+field stores (or a `static inline` seed helper cl won't inline) stuck ~62–84% with wrong this/arg/result register roles + a `call <helper>`; model the seed as a real ctor `T(a,b){m_x=a;m_y=0;…}` (body assignments, not init-list) → `new T(a,b)` recovers retail's regalloc, ~65→99% (CImageSet::CreateFrame24/28/30, CSprite::InsertFrame, CDDrawWorkerMapSmall factories). VPTR POSITION = the last 1%: if the pre-vptr fields are in the BASE, give the base a ctor + delegate (`Derived(a):Base(a){…}`) → the derived vptr lands correctly → 100 EXACT (CDDrawWorkerList CreateWorkerA/B* 55-61→100); if they're the derived class's own, single ctor stamps vptr 1st vs retail Nth → ~99
- [MSVC5 emits a distinct vtable per polymorphic class — RELOC_VTBL is fake](msvc5-per-class-vtables-no-icf.md) — c10 — cpp:vtable cpp:dtor | topic:tooling — every polymorphic class gets its OWN ??_7 (base + every derived, even zero-override/empty/non-virtual-dtor/identical-content); two siblings → three vtables; never-instantiated → none; slot-0 per-class ??_G makes them physically non-identical (unfoldable even with /OPT:ICF, which MSVC5 lacks). No class shares another's vtable RVA → dissolve the ~65 RELOC_VTBL + L_/S_ placeholders to real per-class vtables; sharing a sibling's = UB. 8-experiment toolchain proof + repro.
- [Non-virtual header-inline member craters includers (delinker per-unit packing)](nonvirtual-inline-header-craters-delinker-packing.md) — c8 — cpp:inline cpp:method | topic:wall topic:tooling — moving a scattered NON-virtual multi-caller method into its class header (its true home) emits a COMDAT in every includer-caller; the delinker attributes that rva to the CALLER's unit and packs it into that unit's target .text → craters the caller's real fns (ComputeCellFlags→Brickz.h: BuildRockBreak 81→0, ApplyMove 70→0, ComputeCellFlags LOST). Virtual inline members work (one vtable-emitter TU). DEFERRED on delinker owner-attribution (attribute inline COMDAT to declaring class's unit); keep out-of-line meanwhile (match-neutral)
- [`.bss` sections cap below 100% — objdiff infers COFF symbol sizes from the next symbol's offset](bss-symbol-size-inference-hole.md) — c9 — data:bss data:objdiff | topic:wall topic:scoring-artifact topic:tooling — a .bss section stalls at 99.99% and matched_data (all-or-nothing per SECTION) credits it 0; the residual is always ONE small int, the one cl put at section offset 0. COFF has no symbol sizes, so objdiff INFERS size = next symbol's offset (obj/read.rs infer_symbol_sizes) and diff_bss_symbol scores 100 iff sizes are equal - and MSVC5's .bss hole-filling allocator always parks a 4-byte int in the pad before the first 8-aligned array, so it measures 8. NOT steerable: 3 cl probes prove the .bss layout is declaration-order INVARIANT (depends only on the object set + alignments). ddsurface = 197144 B = 70% of total_data, permanently capped. Do NOT candidate-shape .bss (no bytes exist -> 100% by construction for ANY globals = vacuous) and do NOT add filler globals. Fix is upstream (delinker emits sizes / score bss by declared type), not in src/
- [ILT: retail is linked `/INCREMENTAL`, so a vtable slot holds a `jmp` thunk, not the body](ilt-thunk-indirection.md) — c10 — asm:jmp | topic:delinker topic:scoring-artifact topic:reloc-fidelity — a delinked vtable slot names an unrelated class (`~CTriggerMgr`, `GroupAllScored`, `SetupImage`) or `thunk_FUN_...`, and its `.rdata` can never reach 100%. link.exe /INCREMENTAL routes function-ADDRESS refs through a 5-byte `jmp rel32` band at the start of .text (GRUNTZ: rva 0x1005..0x44a8, 2695 entries, `cc` pad both ends). `cl` cannot name a thunk that does not exist until link, so EVERY DIR32 in the original obj named the BODY - delinking must hop through the thunk (nix/patches/vostok-ilt-thunk-resolution.patch). Corollary: NEVER pin `RVA_COMPGEN`/`RVA()` to a vtable slot's stored VALUE - that pins the symbol onto a 5-byte jmp (14 CUserLogic virtuals did exactly this; real bodies are the 0x87d0..0x89f0 cluster). Reverse audit: invented helpers may chase through one ILT entry to an existing function, into an internal shared tail that calls a real member, or through a `call;ret` wrapper whose consumed EAX proves the member return type (`CueVisible`/`GruntPointVisible`/`BoardTest` -> `PointInBounds`; `EntranceApplyFrame` -> `ApplyLookupSprite`; `GruntTileSwitchImpl` -> `StepArrivalDrop`). Verify every call site independently: one fake name may hide different retail targets or an entire missing branch (`ApplyBox`). Trap: tiny default bodies draw FID LOW false-positives (`__fpclear` claimed at 4 RVAs at once)
- [A generic library/FID label held by a game vtable is a per-class compiler-generated method](library-fid-label-vtable-xref.md) — c10 — cpp:dtor cpp:vtable cpp:comdat | topic:identity topic:mis-model topic:tooling — isolated library color deep inside a game TU; generic `??_G__non_rtti_object`/MFC sibling name on a common scalar-deleting-dtor byte pattern. DNA bands shortlist anomalies, but vtable xrefs decide ownership: source-owned vtable means prune the library row and bind the real per-class `RVA_COMPGEN`; known library vtable means the category survives although the sibling name may still be wrong. Audit: 155/162 generic `??_G__non_rtti_object` rows are proven game-code category errors
- [A PMF registry plus receiver offsets collapses a placeholder method owner](pmf-registry-collapses-placeholder-owner.md) — c10 — cpp:class cpp:member cpp:pmf | asm:call | topic:identity topic:mis-model — bodies known only by high `this+N` offsets were put on a padded fake class, but a typed PMF registry dispatches them on `RealClass*`, every offset maps to that class, and their ILT callees are its existing methods. Fold the placeholder; reverse-audit other ownerless declared-only methods by PMF table, receiver, complete offset map, and absent RTTI/vtable/allocation evidence
- [File-scope object emits a compiler-private `$E<n>` constructor/atexit/destructor family](msvc-static-object-e-helper-family.md) — c10 — cpp:ctor cpp:dtor cpp:static | asm:push asm:call asm:jmp | topic:identity topic:tooling — isolated 14-byte `push <helper>; call atexit; add esp,4; ret` mislabeled LOW `__inc`, with adjacent ctor/dtor tail helpers for one global. Define the real file-scope object, remove hand-written helper twins, and pin its compiler-emitted functions with distinct `_$E<n>` placeholders; normalize both `$E28` and x86 COFF `_$E28` by content + relocations. NetLobby CString family falsified the old recognizer-only test and now exposes unresolved relocation identity
- [Repeated static initializers reveal records, not independent scalar globals](static-initializer-run-reveals-record-layout.md) — c10 — cpp:static cpp:struct | data:bss data:layout | asm:mov asm:ret | topic:identity topic:tooling — nearby addresses are not inherently 16-byte-aligned constants; one tiny `$E<n>` helper per base writes the same `+0/+4/+8` offsets, proving repeated 12-byte constructed records. Grunt direction cells: nine triples recover row/column/direction and the GruntCombat owner
- [Multiple source names at one `.rdata` address mean a pooled literal, not aliased globals](shared-rdata-address-means-pooled-literal.md) — c10 — cpp:static cpp:literal | data:rdata data:layout | topic:identity topic:mis-model topic:reloc-fidelity — raw bytes decode as ordinary FP constants, xrefs only compare/multiply, and two semantic names claim one address: remove the fake globals and spell literals so MSVC recreates the pool. Grunt/path-hazard audit removed four declared-only data aliases and made `CPathHazard::BeginLeg` exact
- [Default-then-conditional-override flag/style — not 0-then-set, not if/else select](default-then-override-flag.md) — c8 — cpp:branch cpp:local | asm:mov asm:je | topic:codegen-idiom — branch polarity je↔jne wrong, or a 2-way constant select folds branchless neg/sbb/and;add
- [signed-char `& 1`: MSVC5 narrows the load to `movb`; retail keeps `movsx`](char-and1-movb-vs-movsx.md) — c7 — cpp:int | asm:mov | topic:wall — retail `movsx eax,byte [r+N]; and eax,1` vs base `movb al,[r+N]; and eax,1`; every spelling of `(signed char flag) & 1` narrows to movb (plain/(int)/temp/inline accessor/+0), 1-instr residual ~95% (CMenuPage::CanWrap 0x183e30)
- [`return Fn(...) != 0;` emits the int→bool neg/sbb normalize](int-to-bool-normalize.md) — c8 — cpp:branch cpp:return | asm:neg asm:sbb | topic:codegen-idiom — trailing neg eax; sbb eax,eax; neg eax missing on a call-result-as-bool return
- [Branchless `(cond ? -1 : 0) & value` guard: retail materializes the AND, our cl fuses to `test reg,reg`](branchless-mask-and-explicit-vs-fused-test.md) — c7 — cpp:branch cpp:local | asm:neg asm:sbb asm:and asm:test | topic:wall topic:regalloc topic:codegen-idiom — `Lookup(k,v)&&v` lowers to neg/sbb mask (steerable via `(…?-1:0)&(i32)v`), but retail `and eax,edx;test eax,eax` (no spill) vs our fused `test ecx,eax` (inline 97.76%) or named-local `and;mov [esp+N],eax` spill (98.57%); 1-region wall
- [Force spill+rewalk of a read-then-write cell: write through the un-cached global](cse-defeat-uncached-global-rewalk.md) — c7 — cpp:local cpp:cse | asm:mov asm:lea | topic:codegen-idiom topic:regalloc — read-then-write of the SAME long pointer-chain cell; retail spills newTok + re-walks m_world->..->m_cells for the write, base CSEs one `lea` addr + holds newTok in a callee-save reg (~62%); read via cached `mgr` but WRITE via un-cached `g_mgrSettings` to defeat the address-CSE → forces the spill+rewalk, DoSwap 0x1128b0 54.9→62→87.6
- [Store the call result in a local before `return x == 0;` for the setcc form](return-bool-via-local-setcc.md) — c8 — cpp:return cpp:local cpp:branch | asm:sete asm:test | topic:codegen-idiom — retail xor edx,edx; test eax,eax; sete dl; mov eax,edx vs your neg/sbb/inc; the named-`hr` local is load-bearing
- [Inline-strcmp `== 0` reject emits setcc only via a `bool` local](strcmp-eq-bool-local-setcc.md) — c8 — cpp:return cpp:local cpp:branch | asm:sete asm:test | topic:codegen-idiom — chained `if(strcmp(name,"X")==0)` rejects: retail xor ecx,ecx; test eax,eax; sete cl; test cl,cl; je vs your test/jne; the local must be typed `bool` (an `int` folds to a branch)
- [Writing an array member through a pointer local defeats flag-CSE (re-read + `mov al,1; test al,cl`)](pointer-store-defeats-flag-cse.md) — c7 — cpp:member cpp:branch cpp:local | asm:test asm:mov | topic:codegen-idiom — two same-condition `if (m_flag&1)` blocks; retail re-reads the flag per test, you CSE it; route index-0 store through a pointer local
- [Writing a contiguous output block: struct-copy, not field-by-field](struct-copy-block-store-base-reg.md) — c8 — cpp:struct cpp:member cpp:local | asm:lea asm:mov | topic:codegen-idiom — a run of adjacent member stores (RECT at obj+0x20) where retail uses `lea base,[obj+N]; mov [base+k]` but your per-field assignment uses full-disp one-at-a-time stores; model the run as a struct member + ONE struct copy; for an inclusive/exclusive off-by-one decrement the dead source rect in place then copy (don't post-adjust fields). 97.5%→98.6% / 99.85% on CImage sprite blitters
- [Missing `sub esp,N` + a dead spill of an UNUSED struct member — model a by-value struct copy](struct-copy-dead-member-store-frame.md) — c9 — cpp:struct cpp:local | asm:mov asm:sub | topic:codegen-idiom topic:regalloc — retail reserves `sub esp,0xc` and dead-stores `mov [esp+N],reg` for a struct member nothing reads; a by-value POD copy (`T cell = *p`) keeps the unused member's load+store, an N-explicit-locals source DCEs it (dropping the frame, desyncing alignment). CGrunt entrance-cell arms: StartBombGruntRun 93→98.7, RearmAttackAnim2 73→95.8, RunEntranceMove 73→82. ONLY when the member is truly dead (if it's read, no spill — reverts)
- [Write the null/early path FIRST to emit test;jne polarity](invert-nullcheck-for-jne.md) — c7 — cpp:branch | asm:test asm:jne | topic:codegen-idiom — je where target has jne on a null/early-out test
- [Early-return zero-guard: spell `if(x <= 0)` not `if(x == 0)` for unsigned `jbe`/`ja`](unsigned-zero-guard-le-not-eq.md) — c9 — cpp:branch cpp:return cpp:type | asm:ja asm:jbe asm:je asm:jne | topic:codegen-idiom — leading `if(count==0)return 0;` on a u32 emits `test;je/jne` but retail emits unsigned `ja`/`jbe`; respell the guard `<= 0` (same value, keeps the early-return so no body-wrap cascade). CRezItm::Read 99.07%→99.77%, Write 98.77%→99.69%
- [Multi-way error layout: nest success-DEEPEST, errors as trailing else-cascade](nested-if-success-deepest-error-tail.md) — c8 — cpp:branch | asm:jcc asm:jmp | topic:codegen-idiom — error blocks inline via jne-skip where target floats them to the tail by forward je, 21-50% on early-returns
- [By-value class arg: trivial copy-ctor INLINES, converting-ctor CALLs — split them](byval-arg-copy-ctor-inline-vs-converting-ctor-call.md) — c7 — cpp:ctor cpp:local | asm:call asm:mov asm:sub | topic:codegen-idiom topic:regalloc — same small class passed by value twice (once from a local RECT, once from *ptr); retail INLINES the local build (4-mov) but CALLs the external converting ctor for the deref; modeling ctor external-for-both adds a persistent temp slot (frame 0x20 vs 0x10, ~53%), inline-for-both drops retail's main-pass call; pass the local as a WapRect lvalue `*(WapRect*)&sh` (trivial copy ctor inlines) + keep `WapRect(*rc)` external (calls). EngStr_RenderText 0x115930 53->60
- [Paired guards with the SAME return: combine with `||` to share the exit block](paired-same-return-guards-combine-or.md) — c8 — cpp:branch cpp:return | asm:test asm:jne asm:je | topic:codegen-idiom — two adjacent `if(a)return X; if(b)return X;` INLINE a full epilogue per guard where retail tail-merges both to one shared exit (two `test;jne`/`test;je` -> one block); combine to `if(a||b)return X;` (an already-`||` guard already merges = the tell); do NOT use /O1 (adds ebp frame+zero-reg). CPlay::HandleMousePress 0xce660 51.7->84.2
- [Identical `return 0` epilogues: retail duplicates, recompile tail-merges](identical-return-epilogue-tailmerge.md) — c7 — cpp:branch cpp:return | asm:ret asm:jmp | topic:wall — body byte-exact but early-out/default zero-returns jump to one shared pop;pop;ret tail (xor eax,eax delayed between pops) where retail emits the clean 3-instr epilogue inline at each site, ~60-70%
- [Guard before a loop: `if(cond){loop}` falling through to the single return, NOT `if(!cond) return acc;`](guard-skip-loop-not-early-return.md) — c8 — cpp:branch cpp:return cpp:loop | asm:jcc asm:jmp asm:ret | topic:codegen-idiom — a mid-fn guard that early-returns the running accumulator emits a DUPLICATE pop/ret epilogue where retail `jg`s to the shared exit; gate the loop instead, 95%→99.7% (mirror of identical-return-epilogue-tailmerge)
- [Long homogeneous short-circuit chain: collapse to one `&&` (shared return-0 tail), NOT per-rung `if(!Fn())return 0;`](homogeneous-predicate-chain-and-shared-tail.md) — c8 — cpp:branch cpp:return | asm:test asm:je asm:ret | topic:codegen-idiom — N identical predicate rungs that each abort: retail shares one `je <fail>` tail, but per-rung `if(!Fn())return 0;` INLINES N epilogues, bloating size so objdiff stops pairing (no %); `if(a && Fn0 && … && Fn_n) return 1; return 0;` lands 100% (CSpriteRefTable::LoadToolToyPalettes 0xe2980). Caveat: a per-rung side-effect store breaks the merge → retail inlines → keep the if/return (BuildToolToyColorTable 0xe2400)
- [`void`-modeled function won't split epilogues — declare the real `int`/BOOL return](void-vs-bool-return-epilogue-split.md) — c8 — cpp:branch cpp:return | asm:ret asm:mov | topic:codegen-idiom — void recompile tail-merges identical bare pop;ret guards + omits mov eax,1; real int return restores retail's inline per-site epilogues + eax=1 tail, ~82-85%→100% (RVA-keyed pairing ignores the QAEXH vs QAEHH mangling)
- [Short-circuit bool arg beside a coincident constant: if/else with literal args, not inline `||`](boolarg-coincident-const-ifelse-literal.md) — c8 — cpp:branch cpp:local | asm:push asm:mov | topic:codegen-idiom — `f(K, A||B||C)` where K equals the bool's true value shares eax (`mov eax,1;push eax;push eax`, 3B long + size-mismatched); split to `if(A||B||C) f(K,1); else f(K,0);` for two literal pushes (true) + reused je-zero `push eax` (false). CBattlezDlg::ApplyOption0..3 91.84%→100% (GetDlgItem(1)->EnableWindow)
- [Lone `cond ? c1 : c2` bool call-arg: if/else with literal args to force the branch-push (not `sete`)](boolarg-branch-push-not-sete.md) — c8 — cpp:branch cpp:local | asm:sete asm:push asm:jmp | topic:codegen-idiom — a single bool arg `f(cond?1:0)` folds to `xor;test;sete al;push eax` (+hoists the receiver load) where retail branches `test;je;push imm;jmp;push imm`; write `if(cond) f(0); else f(1);` (cl tail-merges to one call, receiver loaded after the branch). CPlay::PauseGame 0x0cee90 61%→100%; an intermediate int local still folds to sete
- [`base + bool` arg held in a register: asymmetric init + one-sided `if` (not ternary/symmetric if-else)](flag-base-plus-bool-asymmetric-ifelse.md) — c8 — cpp:branch cpp:local | asm:setne asm:mov asm:jmp | topic:codegen-idiom — an arg `base+(cond?1:0)` (base!=0, shared in a multi-arg push list) folds to `xor;test;setne al;add base` for ternary AND symmetric if/else; write `int x=base; if(cond) x=base+1;` to get retail's register branch `mov eax,base;je;mov eax,base+1` + one shared push. winapi_115300_SetRect 0x115300 94.9%→100%
- [Shrink-wrapped callee-save push — retail defers `push edi`/`push ebx` past a cheap null guard](shrink-wrapped-callee-save-push.md) — c7 — cpp:branch cpp:return | asm:push asm:pop | topic:wall topic:regalloc — early null-guard returns saving only ebp/esi (`xor eax,eax;pop esi;pop ebp;ret`) then `push edi;push ebx` AFTER the guard; recompile pushes all four upfront, ~85-90% body-exact, not source-steerable
- [`& ~0x1f` align-down emits `and al,0xe0` (byte) — write `& ~0x1f` not `& 0xe0`](align-down-byte-and-encoding.md) — c7 — cpp:bitand | asm:and | topic:codegen-idiom — target uses byte `and al,0xe0` for one axis, dword `and r32,0xffffffe0` for another, same align-down
- [`(dwordVar & bit) == bit` bool: retail keeps dword load + byte AND, no spelling gives the mix](dword-load-byte-and-mix.md) — c7 — cpp:bitand cpp:int | asm:and asm:mov | topic:wall topic:regalloc — 1-instr residual `and eax,imm` (ours) vs retail `and al,imm` while both `mov eax,[mem]`; (u8) cast flips the AND but wrongly narrows the load to `mov al`; ~99% (DirectSoundMgr::IsLooping/IsPlaying)
- [strcpy/memset/struct-copy inline to rep movs/stos at /O2 /Oi — write the plain call](inline-mem-ops-rep-movs.md) — c8 — cpp:builtin | asm:rep | topic:codegen-idiom — rep movsd/movsb / repne scasb / rep stosd where you expected call _strcpy/memset
- [Force an out-of-line `call _memcpy` (vs rep movs) with `#pragma function(memcpy)`](force-outofline-memcpy-pragma-function.md) — c8 — cpp:builtin | asm:rep asm:call | topic:codegen-idiom — retail does `call memcpy` for a runtime-variable copy but your recompile inlines `rep movsl/movsb`; pragma per-TU disables the intrinsic (GrowTo 62→80%)
- [Variadic `...` fn: use va_list/va_start/va_end, not (char*)(&lastarg+1)](variadic-va-start-not-ptr-math.md) — c8 — cpp:vararg cpp:cast | asm:lea | topic:codegen-idiom — reconstructing a `...` fn; va_start lowers to the same lea arg-ptr as the &lastarg+1 hack (matching-neutral) and is the real shape
- [Tiny inline ctor (bare vptr store) has no source-recoverable standalone COMDAT](comdat-inline-ctor-no-standalone.md) — c7 — cpp:ctor cpp:inline | asm:mov | topic:comdat topic:wall — 7-byte mov [this],&vftable; ret, no mov eax,ecx, no null guard
- [Repeated/large store constants: retail materializes into regs, cl keeps immediates](const-materialize-into-reg-vs-immediate.md) — c7 — cpp:ctor cpp:local | asm:mov asm:xor asm:test | topic:wall topic:regalloc — same offsets+values but reordered; `mov eax,0x140;mov ecx,0xf0` vs immediate stores; `xor edi,edi`+`cmp[..],edi`/`mov[..],edi` vs `test`+`mov[..],0`, cascading index-reg pin; CFxModeT2 ~72% / CDDPageMgr::RemoveAt ~86%, siblings 100%
- [Reloc-typing: vptr/global/IAT operands differ, code bytes match](reloc-typing-vptr-global.md) — c9 — cpp:ctor cpp:member | asm:mov asm:call | topic:scoring-artifact topic:tooling — ~99.5% fuzzy 0 structural diffs, ??_7/__imp__/DAT_ operand slots
- [Multi-member ctor EH state-numbering base shifts on incomplete TU](eh-state-numbering-base.md) — c7 — cpp:ctor cpp:eh | asm:mov | topic:wall topic:eh — body byte-identical, residue in __ehfuncinfo state ids, ~89-95%
- [/GX method with a scoped destructible local: EH frame 4 B short + scope cookie differs](gx-scoped-local-eh-frame-size.md) — c6 — cpp:eh cpp:local | asm:push asm:mov | topic:wall topic:eh — body byte-identical, retail sub esp,0x44 / push 0x8 vs recompile 0x40 / push 0x0, [esp+N] shifted 4, ~86%
- [/GX ctor: leaf vptr re-stamp lands in EH state 0, not the entry state](eh-ctor-vptr-restamp-position.md) — c7 — cpp:ctor cpp:eh cpp:virtual | asm:mov | topic:wall topic:eh — one mov [esi],vtbl scheduled ~4 instrs late vs retail's eager post-base stamp, ~94-95%
- [Throwing `::operator new` emits the member-ctor /GX state transition a nothrow C alloc omits](throwing-operator-new-eh-state-transition.md) — c8 — cpp:ctor cpp:new cpp:eh | asm:mov asm:call | topic:codegen-idiom topic:eh — missing `mov BYTE [esp+N],<state>` before the heap-alloc call in a /GX ctor; spell the buffer alloc `::operator new` not the nothrow `extern "C"` RezAlloc alias, 95.95→97.33% (CSymTab ctor 0x139de0)
- [/GX frame from an OUT-OF-LINE base ctor call in a `new CSBI_X` builder](gx-frame-outofline-ctor.md) — c8 — cpp:new cpp:ctor cpp:eh | asm:call asm:push asm:mov | topic:codegen-idiom topic:eh — per-item `new CSBI_X` builder scores 0% (no /GX frame) though logic is done; the item ctors are TINY (stamp vtable+zero fields, no destructible members) and retail CALLS the base ctor OUT OF LINE — the opaque may-throw call raises the operator-delete-on-throw frame; declare the shared base view's ctor out-of-line (not inline-folded) to emit `call ??0base` + the Order-A prologue, BuildGameMenu 37%→63%; helps only a COMPLETE body (a large partial regresses 23.7%→20.6%, Order-B this→ebp); residual: new-temp non-fusion (+8 B frame) + EH-state rotation; CHECK UNIT FLAGS FIRST - the ~32% "cannot re-raise" pump wall was a flags="base" mis-profile (no /GX at all); under eh + unsigned switch key the pumps hit 100% (wave2-H)
- [A `new`d unmatched engine class must be padded to its REAL size](newd-class-real-size.md) — c8 — cpp:ctor cpp:new | asm:push asm:call | topic:codegen-idiom — push <wrong-N> to operator new desyncs from the alloc onward, thin shell pushed 0xc
- [Opened scalar `new T(...)` shows as `operator new` + null-guarded ctor](new-null-guarded-ctor-opened.md) — c8 — cpp:new cpp:ctor | asm:call asm:test asm:jcc | topic:codegen-idiom — decompiler shows `operator new(size); if(p) p=p->Ctor(...); else p=0`; target asm is push size; call ??2; test; je; mov ecx; call ctor; write real `new T(args)` with inline ctor/fixed-size class operator new only when exact/fuzzy/README metrics hold
- [`new T[n]` (T non-trivial) → array-cookie alloc + `__ehvec_ctor` + /GX frame — model it as a real array-new](array-new-cookie-ehvec-ctor.md) — c8 — cpp:new cpp:ctor cpp:eh | asm:push asm:call asm:lea | topic:codegen-idiom topic:eh — `lea eax,[n*8+4]`;operator new;`mov [eax],n`(cookie);`lea ebx,[eax+4]`;call `??_L`, all in a /GX frame; a manual alloc+extern __ehvec_ctor reproduces the body but NOT the frame (~60%); element needs a non-trivial ctor+dtor; iterators are __stdcall
- [A record callback calling `__ehvec_ctor` proves an array member, not adjacent fields](ehvec-member-array-not-adjacent-fields.md) — c10 — cpp:array cpp:member cpp:ctor cpp:dtor cpp:eh | asm:push asm:call | topic:codegen-idiom topic:identity — record ctor/dtor each make one helper call with count N + sizeof(T), but source with N adjacent non-trivial fields expands a large per-member /GX state chain; regroup as `T member[N]` and retain meanings through inline reference accessors (CGruntCellRec 0xf400/0xf430)
- [A /GX builder's `this`-register (esi vs ebp/edi) is decided by the WHOLE body's register pressure](gx-this-esi-via-cache-store-pressure.md) — c8 — cpp:eh cpp:new cpp:ctor cpp:member | asm:mov asm:push | topic:regalloc topic:eh topic:codegen-idiom — big /GX per-item `switch` builder plateaus low-20s% as a partial; out-of-line base ctor raises the /GX frame but lands this->ebp/edi (Order-B) not retail's `mov esi,ecx`. STEERABLE by reconstructing the WHOLE body: the esi-relative cache stores (`this->m_slotN = it`) make `this` the hottest ptr (pins it to a callee-saved reg — flips this->esi even w/o /GX), and the full item runs' ~50 uses of the 0-constant dedicate edi=0 which spills `code` to `[esp+0x10]` and frees esi. Don't judge the layout on a partial (looks like a 24->20.6% regression); reconstruct the runs first (LoadTabSprites 0x102250 24->44.8%, prologue now byte-exact Order-A; each run +8.5/+5.3/+3.5/+1.9%). Residual: frame 0x28 vs 0x34 (missing loop induction locals)
- [RezAlloc + placement-new ctor: MSVC5 omits the retail /GX EH frame](rezalloc-placement-new-no-eh-frame.md) — c8 — cpp:eh cpp:new cpp:ctor | asm:mov asm:push | topic:wall topic:eh — push -1/fs:0 prologue + shared `jmp` epilogue MISSING, body byte-exact, sub esp 0x28 vs 0x34, zero in edi vs esi, ~47%; UPDATE: with the ctor's class REALLY derived, plain `new T` emits the retail frame (prologue byte-exact) — residual is only epilogue tail-merge + one ebp pin
- [`new T`-with-throwing-ctor: the ctor-in-flight unwind funclet is appended to the body, not section-split](new-throwing-ctor-unwind-funclet-appended.md) — c7 — cpp:new cpp:ctor cpp:eh | asm:call asm:jmp asm:ret | topic:wall topic:eh — `new T` (T has a throwing ctor) emits the retail /GX frame + body byte-exact to the `ret`, then an EXTRA out-of-line cleanup funclet (`mov eax,[ebp-N];push;call ~T;ret;mov eax,0;jmp`) is appended past the delinked range, ~95-97% (CDDrawSubMgrAni::CreateAniEntry 0x1528d0); inverse of rezalloc-placement-new (frame PRESENT not absent)
- [Multiply-derived `new`: model the DERIVED ctor INLINE so 2 vtable stores land at the new-site](inline-multiderived-ctor-vtable-stores.md) — c7 — cpp:ctor cpp:inline | asm:mov | topic:codegen-idiom — op_new; ctor; mov [node],vtblA; mov [node+8],vtblB inline at new-site
- [Re-run a ctor in place: explicit `obj.Class::Class()` gives a clean tail-jmp; placement-new adds a null-check](explicit-ctor-call-inplace-tail-jmp.md) — c9 — cpp:ctor cpp:new | asm:jmp asm:mov asm:test | topic:codegen-idiom — standalone 0xa-byte `mov ecx,offset g; jmp Ctor` (no null-check); `new (&g) T` keeps the placement null-guard `test eax,eax; je` (12B, 0%); explicit-ctor-call extension `g.Class::Class()` is the clean tail-jmp → exact, lets the global type to its REAL class + kills the hex view (Forwardb5400 0xb5400)
- [Emit a class's ??_7 vtable in-TU by declaring ALL virtual slots (stubs for unmatched)](emit-vtable-in-tu.md) — c8 — cpp:ctor cpp:virtual | asm:mov | topic:codegen-idiom — ctor vptr store has no symbol to bind / Q→U mangling flip on full vtable
- [__thiscall foreign vtable: declared-only virtual interface is the ONLY MSVC5 form — char-pad fn-ptr Vtbl struct impossible](thiscall-vtable-declared-only-not-charpad.md) — c9 — cpp:virtual cpp:method | asm:mov asm:call | topic:wall topic:codegen-idiom — C4234 "'__thiscall' keyword reserved for future use" on a fn-ptr; the vNN/dummyN/SlotN placeholder run for a `mov eax,[this];mov ecx,this;call [eax+off]` slot is FORCED (char-pad Vtbl structs only model __stdcall/COM vtables); WwdSelf/CSerialArchive/CPlane/CPlaneRenderPoly
- [Define `~Class()` INLINE in the header to match an inlined `??_G` scalar-deleting dtor](inline-vdtor-for-scalar-deleting.md) — c8 — cpp:dtor cpp:eh cpp:inline | asm:call asm:test | topic:codegen-idiom — your ??_G does call ??1 where target inlines (store vtbl; call base; test [esp+8],1; delete; ret 4)
- [Inline the trivial base dtor so leaf `??1` destructors FOLD the base teardown (pin the base COMDAT with RVA_COMPGEN)](inline-base-dtor-folds-into-leaves.md) — c9 — cpp:dtor cpp:eh cpp:inline cpp:virtual | asm:mov asm:call | topic:codegen-idiom topic:eh — family of identical /GX leaf dtors each fold the SAME base teardown inline (no call ~B); shared-header body + ascending `RVA_COMPGEN` standalone pin; wrong visibility gives vptr+jmp at 4.44%, corrected form makes both exact
- [One surface hierarchy, five byte-identical COMDAT dtors under five names](surface-pool-comdat-dtors.md) — c9 — cpp:dtor cpp:vtable cpp:comdat | asm:mov asm:call | topic:wall topic:identity topic:scoring-artifact — five `??1C...@@UAE@XZ` at 5 RVAs with IDENTICAL bytes (`mov [esi],0x5ef7f0; call 0x13e4d0; lea ecx,[esi+0x94]; call 0x1b4f3e`) are NOT copies of one class: they are 1 base + 4 derived (vtables 0x5ef7f0/a58/a88/ab8/ae8, RTTI-less g_poolItemVtbl), byte-equal only because the derived vptr stamp folds; OWNER is fixed by each vtable's slot-0 ??_G→~ (dump .rdata), not the bytes; base modeled 3× (CDDSurface/CFileImage/CPoolItemBase), a58 2× (CFileImageSurface/CPoolItemA); pool-B = CDDPalette (proven by method-RVA eq), not a surface
- [Force the standalone COMDAT of an inline base ctor, and isolate a depth-dependent inline callee in its own TU](inline-base-ctor-standalone-comdat.md) — c8 — cpp:ctor cpp:eh cpp:inline | asm:call asm:mov | topic:codegen-idiom topic:eh — ctor analog of the dtor fold: an inline base ctor whose OWN out-of-line copy also exists in retail has NO synthesized caller, so force it with `#pragma inline_depth(0)` (small) / repeated `new` (big) + `RVA_COMPGEN`; if the standalone must INLINE a helper the leaves CALL, put the forcer + inline body in a SEPARATE unit (else it folds into every leaf at depth 2 and regresses them); guard leaf-only member stores with `#ifndef …_STANDALONE_CTOR`; CUserLogic ctors 0x138d0 5%->100% / 0x58cd0 0.9%->89% with 0 leaf regression
- [Destructor's /GX EH frame comes from a non-trivial base subobject — unreachable with a manual-vptr non-polymorphic model](eh-dtor-needs-base-subobject.md) — c7 — cpp:dtor cpp:eh cpp:virtual | asm:mov | topic:wall topic:eh — retail ~Dtor has push -1/mov fs:0 + [esp+0x10]=1/-1 trylevel around the body and a trailing base-dtor call; recompile is frameless (no fs:0, no trylevel), body otherwise exact, ~50-60%
- [/GX member-teardown dtor: model the destructible MEMBERS as real subobjects with destructors](eh-dtor-model-members-as-destructible.md) — c9 — cpp:dtor cpp:eh cpp:member | asm:mov asm:call | topic:codegen-idiom topic:eh — descending [esp+N]=3/2/1/0/-1 trylevel chain around N reverse-order `call ~Member`; a hand-rolled `Dtor(){m_a.Dtor();...}` is frameless (~42%); give each member type `~T(){EngineDtor();}` + write a real `~Class()` -> compiler emits the whole frame, 42%->100% (CTileTriggerContainer 0xc8640); steerable counterpart of eh-dtor-needs-base-subobject
- [/GX dtor: an embedded sub-object's trailing vptr re-stamp is its MEMBER destructor](eh-dtor-subobject-vptr-restore-member.md) — c8 — cpp:dtor cpp:eh cpp:member | asm:mov | topic:codegen-idiom topic:eh — manual-vtable class; ~Class ends with `mov [this+N],<base-vtbl>` AFTER the last member RemoveAll; that store is the embedded sub-object's member dtor (give the sub-object `~T(){m_vtbl=&base;}`, declare it a value member, drop the trailing store from the body), 79.7%->98.0% (CSymParser 0x13abc0); pairs with eh-dtor-model-members-as-destructible
- [/GX dtor re-stamps the most-derived vptr but retail did not: a MIS-MODEL, not a wall](eh-dtor-vptr-restamp-presence.md) — c10 — cpp:dtor cpp:eh cpp:virtual cpp:implicit-member | asm:mov | topic:mis-model topic:phantom-wall topic:codegen-idiom topic:eh — one extra mov [esi],&??_7Class re-stamp before the trylevel write, ~92-93%; vtable_scan picks the cause: NO vtable => the class isn't polymorphic (de-inherit); HAS a vtable => retail's dtor is COMPILER-GENERATED and yours is user-declared (delete the decl, pin with RVA_COMPGEN)
- [/GX dtor: entry trylevel-0 write scheduled BEFORE the vptr re-stamp vs retail's stamp-first](eh-dtor-vptr-stamp-vs-trylevel-order.md) — c7 — cpp:dtor cpp:eh cpp:virtual | asm:mov | topic:codegen-idiom topic:eh — the compiler-gen `mov [esp+N],0` and the user `mov [this],<vtbl>` emitted in opposite order, body otherwise byte-identical, ~94%; STEERABLE via eh-dtor-implicit-vptr-stamp-first (compiler-implicit stamp lands stamp-first)
- [/GX dtor: a COMPILER-implicit vptr stamp emits stamp-FIRST — converts the manual-stamp order wall](eh-dtor-implicit-vptr-stamp-first.md) — c9 — cpp:dtor cpp:eh cpp:virtual | asm:mov | topic:codegen-idiom topic:eh — manual `*(void**)this=&g_vtbl` is a body store scheduled after the trylevel-0 write (~94%); make the class real-polymorphic (`virtual ~Class()`, drop the manual stamp) so cl emits the implicit stamp-first; empty non-trivial base dtor + derived-body resets land the grand stamp last; CImage/CFileImage/CFileImageSurface/CImgHolder 94→100
- [/GX dtor inlining a member's MANUAL-vtable-stamp teardown: the member-`this` adjust (`add esi,4`) + trylevel value don't reproduce](eh-dtor-inline-member-vtable-stamp-thisadjust.md) — c7 — cpp:dtor cpp:eh cpp:member cpp:inline | asm:add asm:mov | topic:wall topic:eh — modeling the embedded subobject as a real destructible member recovers the /GX frame (41%→80%) but inline keeps esi=this (no `add esi,4`/`[esp+4]` re-point, state 0→-1 not 0→1); out-of-line flips ctor 100%→7%; clean exit needs an external member dtor not manual stamps
- [Dense x87 FP expression: the fxch stack schedule is not steerable from C](x87-fp-stack-schedule.md) — c7 — cpp:float | asm:fxch asm:fsin asm:fcos | topic:wall — multi-term double expr (sin/cos) lowers to fld/fxch/fmul/faddp whose stack ordering diverges from retail; integer scaffolding + __ftol stores byte-exact, ~60-75%
- [A repeated FP block: copy-paste it (macro), don't `__inline` a helper](x87-copypaste-vs-inline-fp-block.md) — c7 — cpp:float cpp:inline cpp:macro | asm:fld asm:fxch asm:fmul | topic:codegen-idiom topic:scheduling — N identical per-axis/per-channel x87 blocks: an `__inline` helper x N over-schedules into one deep interleaved region (~35%), a `#define BLOCK(...) do{}while(0)` macro x N schedules each block locally w/ shallow stack like retail's copy-paste (CMotionState::Step 0x16ecd0 35%→65%, prologue+control-flow+stores exact); per-block fxch residual = the x87-fp-stack-schedule wall
- [x87 zero-clamp compare: put the constant on the LEFT to preload it](x87-clamp-compare-operand-order.md) — c7 — cpp:float | asm:fcomp asm:fldz asm:fxch | topic:codegen-idiom — `if (disc < 0.0)` before fsqrt reloads zero via `fcoml [mem]`; retail preloads it and does reg-reg `fcomp st(1)`; flip to `if (0.0 > disc)` (constant-left) to schedule the early `fld` (isolated FP fns only; CMotionState::ArrivalVelX/Y 0x16f3c0/0x16f430 88%→100%)
- [bool return spanning the SEH-frame teardown is re-canonicalized — spell `? true : false`](seh-bool-return-canonicalize.md) — c8 — cpp:eh cpp:branch | asm:test asm:setne | topic:codegen-idiom — trailing test al,al; setne al present in retail absent in recompile, interleaved with the /GX cleanup, ~97%→100%
- [Destructible stack local forces the /GX frame → flags="eh"](gx-frame-destructible-local.md) — c9 — cpp:eh cpp:local | asm:mov | topic:flags topic:codegen-idiom — whole __except frame MISSING under base flags, CString/CFileIO/RegistryHelper local
- [Split the TU when one method needs /GX but a sibling's CString-return must stay frameless](split-tu-eh-dtor-vs-frameless-cstring.md) — c8 — cpp:eh cpp:dtor cpp:local | asm:push asm:mov | topic:flags topic:codegen-idiom — dtor 100% under eh but a GetKey()-by-value sibling gains a push -1/fs:0 frame the target lacks; base un-frames the dtor; split dtor into its own eh TU
- [Scope a destructible temp to elide the /GX EH frame when no throwing call is live during it](scope-cstring-temp-to-elide-eh-frame.md) — c8 — cpp:eh cpp:local cpp:dtor | asm:push asm:mov | topic:codegen-idiom topic:eh — spurious push -1/fs:0 frame around a CString temp that retail lacks; wrap the temp in an inner { } so it dies before the later (throwing) calls, 59→93%
- [Large branchy /GX function: objdiff fuzzy% unreliable (alignment desync) — read diff_kind histogram](big-seh-fuzzy-desync.md) — c7 — cpp:eh | topic:wall topic:tooling topic:scoring-artifact — 3000-B SEH fn rolls up 0.0% despite faithful carcass, INSERT/DELETE pairs dominate
- [/GX state-machine error paths destroy the local via the scalar-deleting dtor (re-stamp vtbl + call [vtbl+4]), not simple ~T](gx-state-machine-scalar-delete-cleanup.md) — c7 — cpp:eh cpp:dtor cpp:local | asm:call asm:mov | topic:wall topic:eh — big /GX serialize fn: each reject emits `mov [esp+N],<scalardel-vtbl>; call ds:[vtbl+4]` + even/odd EH-state pair before a shared ~T tail; idiomatic `return 0;` emits only simple ~T, fail ladder desyncs (~74.6%, CDDrawSurfaceMgr::SnapshotChildren 0x156020)
- [Win32 imports: bring them from the real headers (<Mfc.h>/<Win32.h> → windows.h) → FF15 [IAT]](win32-import-decl-stdcall.md) — c9 — cpp:import | asm:call | topic:codegen-idiom topic:flags — spurious add esp,N after a cdecl call, or a hand-rolled typedef/extern drifting from the real Win32 signature (umbrellas are matching-neutral)
- [The "MFC C1189 wall" is breakable: a <Win32.h> TU that needs MFC types switches to <Mfc.h>](mfc-wall-is-breakable-switch-to-mfc.md) — c9 — cpp:mfc cpp:include | topic:tu-layout topic:codegen-idiom — do NOT keep a placeholder CMulti/CPtrList/CString view or `*(i32*)((char*)p+off)` cast "because of the wall"; <Mfc.h> is a SUPERSET of <Win32.h> (afx.h pulls the same windows.h). Replace <Win32.h> with <Mfc.h> (kept FIRST) + include the real class; matching-neutral (OnReset 0xc3e30 62→100). Caveats: STRICT needs (HWND) on loose PostMessageA ptrs; delete any local decl-only `struct CMulti` proximity-host
- [Using <afxwin.h> (CWnd/CRgn/CGdiObject): undef _AFX_ENABLE_INLINES for clang](afxwin-clang-label-step-skip-inl.md) — c9 — cpp:mfc cpp:include | topic:tu-layout topic:toolchain topic:codegen-idiom — <Mfc.h> excludes afxwin.h; pulling it to fold a CWnd/CRgn/CGdiObject view builds under wine cl but the `gen_labels` clang step dies (`afxwin1.inl:1027 a type specifier is required`, implicit-int CMenu::operator==). Fix: `#ifdef __clang__ #undef _AFX_ENABLE_INLINES #endif` before <afxwin.h> (label step needs only declarations; wine cl keeps the inlines). Retires the "CWnd can't be pulled" exception. Matching-neutral (CreditzRgn→CRgn, BuildText 99.90%)
- [Lookup out-param zero-init SINKS past arg pushes — scheduling wall](outparam-zeroinit-scheduling.md) — c8 — cpp:local | asm:mov asm:push | topic:wall topic:scheduling — identical multiset 2-3 instrs permuted per Lookup(name,&out), 80-95%
- [Retail pins 0/1 in a callee-saved register — regalloc wall](zero-register-pinning.md) — c7 — cpp:local | asm:xor asm:mov | topic:wall topic:regalloc — 1-instr phase shift through every =0/=1 store, ebx/edi/ebp zero-reg
- [Zero-register pinning IS steerable when a CALL splits the seeds — write pre-call seeds first](seed-zero-fields-before-call-callee-saved.md) — c8 — cpp:local cpp:branch | asm:mov asm:xor asm:call | topic:codegen-idiom topic:regalloc — field-seed method (Clear/Reset/ctor) with a member op=/ctor CALL amid the =0 stores stuck ~50%; base pins zero in caller-saved eax, retail keeps it in callee-saved edi ACROSS the call; move the fields retail seeds pre-call ahead of the call → zero must survive the call → cl pins it callee-saved (GruntzPlayer::Clear 0xda960 52→94.65). Distinct from the call-LESS wall (no reorder flips that)
- [Pin a named local (or split a pointer-chain) to force MSVC's reg/schedule — arg-count-dependent](pin-local-for-callee-saved-reg.md) — c8 — cpp:local | asm:mov asm:push | topic:codegen-idiom topic:regalloc — target caches a value callee-saved across calls / interleaves a load between arg pushes; inner-split helps 3-arg but hurts 2-arg
- [Re-read a member-view pointer per access — don't cache it in a local](reread-member-view-pointer.md) — c8 — cpp:local | asm:mov | topic:codegen-idiom topic:regalloc — target re-loads `mov reg,[this+N]` before every sub-access; a cached `T* v=m_N` pins it callee-saved + adds a push, teardown/walk ~70-80%→~99% on re-deref
- [Statement order scheduled faithfully: store-before-call, member-init order, local-array decl position](statement-schedule-faithful.md) — c8 — cpp:local | asm:mov asm:push | topic:codegen-idiom topic:scheduling — a byte shifts when a store moves vs a call; a string-literal copy scheduled too early
- [Stack-buffer SIZES drive the frame size + slot offsets — match the declarations](stack-buffer-size-drives-frame.md) — c8 — cpp:local | asm:sub | topic:codegen-idiom — wrong `sub esp,N` or a buffer/object lands at the wrong [esp+M]
- [Two mutually-exclusive branch locals: retail keeps DISTINCT slots, cl COALESCES → frame 4 B short](stack-slot-coalesce-frame-4b.md) — c6 — cpp:local cpp:branch | asm:sub asm:lea | topic:wall topic:regalloc — body byte-identical, `sub esp,N-4` vs retail N, every [esp+M] shifted 4, return spill vs eax, ~88%
- [A class with an explicit `m_vptr` field must NOT also get virtuals — shifts every offset by 4](explicit-mvptr-no-virtuals.md) — c8 — cpp:member cpp:virtual | asm:mov | topic:codegen-idiom topic:tu-layout — adding virtual regresses exact leaves, m_0c→+0x10
- [Non-EH leaf ctor storing a hand-rolled vtable LAST must keep the manual `m_vtbl` field — virtualizing forces the vptr stamp FIRST and regresses](ctor-handrolled-vptr-store-last.md) — c9 — cpp:ctor cpp:virtual cpp:member | asm:mov | topic:wall topic:codegen-idiom — WAP `z*` node (zPTree/zDArray); retail `mov [this],&vtbl` AFTER member stores, `virtual ~T()` moves it to ctor entry, 100%→82.9% (CButeNodeEntry 0x16df70, proven llvm-objdump); non-RTTI (engine type-descriptor, not /GR)
- [Subsystem GetErrorString/ReportError is static __cdecl, not __thiscall](geterrorstring-static-cdecl.md) — c9 — cpp:method cpp:static cpp:switch | asm:push asm:call | topic:codegen-idiom topic:archetype — push×3; call; add esp,0xc; ret, Ghidra says void __thiscall(void)
- [Chained `f(a)->g(b)` hoists the outer arg early — split to a temp to defer it](chained-call-arg-eval-order.md) — c8 — cpp:method cpp:call | asm:push asm:mov | topic:codegen-idiom — outer arg pushed before the inner call (kept live across it); a named temp makes retail reload it after, ~49→100%
- [A "function" whose stack does not balance is NOT a function — check the boundary](unbalanced-stack-means-wrong-boundary.md) — c10 — asm:push asm:pop asm:ret | topic:wall topic:boundary topic:scoring-artifact — pushes vs pops disagree against `ret N`; args seem to "arrive already in callee-saved regs"; no caller/vtable/thunk references it; a 30-40% plateau no spelling moves. Balance the stack BEFORE believing any "no source form reproduces this ABI" wall — the block is a fragment of a bigger function (replaces the fabricated serialize-wrapper-reg-forward ABI note)
- [MFC AFXAPI helpers (CString operator+, Format) are __stdcall, not __cdecl](afxapi-operator-stdcall.md) — c8 — cpp:method cpp:mfc | asm:call asm:add | topic:codegen-idiom — spurious add esp,0xc after each CString operator+/concat, stack-temp offsets cascade, ~3-5% low across a family
- [An MFC message map is two typed static objects, not an integer placeholder](mfc-message-map-real-static-data.md) — c10 — cpp:mfc cpp:static cpp:virtual | asm:mov | topic:mis-model topic:codegen-idiom topic:tooling — official `BEGIN_MESSAGE_MAP` expansion is an 8-byte `AFX_MSGMAP` plus 24-byte terminator entry; first pointer relocates to the base map, second to the entry array. For `DATA` binding spell the typed expansion explicitly; clang's `@@0QBUAFX_MSGMAP_ENTRY` candidate resolves to VC5's `@@0PBU...` only under object-symbol authority
- [Declare an unmatched callee as external no-body method to reloc-mask the call; convention kills stack cleanup](external-nobody-callee.md) — c9 — cpp:method | asm:call | topic:codegen-idiom topic:archetype — need a reloc-masked call rel32 / FF15 [IAT] to an engine fn you are NOT matching
- [Declare N dummy virtuals so the real method lands at the right vtable offset](dummy-virtual-slots.md) — c8 — cpp:method cpp:virtual | asm:mov asm:call | topic:codegen-idiom — need mov edx,[ecx]; call [edx+0xNN] but the method is at the wrong index
- [Pointer-to-member-function call: define the class BEFORE the PMF typedef so it stays 4 bytes](pmf-complete-class-4byte.md) — c9 — cpp:virtual cpp:member | asm:call asm:mov asm:add | topic:codegen-idiom topic:regalloc — `(this->*(e->m_fn))()` on an incomplete class emits an extra `mov ecx,[entry+4]`+`add ecx,this` (8-byte PMF) vs retail's bare `mov ecx,this; call [entry]`; define the class above the `T::*` typedef, 93.7%→100%
- [Pick the calling convention by the `ret N` in the target](ret-n-calling-convention.md) — c8 — cpp:method cpp:static | asm:ret | topic:codegen-idiom topic:archetype — wrong/extra stack cleanup; a static @@SA (cdecl) where the target callee-cleans (@@SG)
- [Recover a hidden member from the caller's `mov ecx`](thiscall-ignoring-this.md) — c10 — cpp:method cpp:static | asm:mov | topic:codegen-idiom — body never reads this, or a free-looking function calls methods without loading ecx; callers preload the same real object, proving the function is a member and its threaded helper is a phantom
- [MFC-derived (CFile-clone) code is built /O1 (optimize SIZE), not /O2 — flag tell](o1-for-mfc-derived.md) — c8 — cpp:mfc | asm:push asm:mov | topic:flags topic:codegen-idiom — MFC-shaped fn plateaus 60s-80s% under /O2; pushes mem operands directly, ebp frame on address-taken locals
- [A game class that USES MFC types is still /O2, not /O1 — flip mfc→eh](o2-for-mfc-using-game-class.md) — c9 — cpp:mfc | asm:mov asm:and | topic:flags topic:codegen-idiom — MFC-using TU plateaus 40s-90s% under /O1; `and $0,mem`, tail-merged return epilogues, jmp-to-shared-ret in leaf accessors (the /O1 tells)
- [rand() % var won't reproduce MSVC5's divisor-guard modulo-peel](rand-modulo-peel.md) — c8 — cpp:modulo cpp:rand | asm:idiv asm:test asm:jcc | topic:wall — ~180 extra TRGT instrs, test divisor,divisor; je guard before idiv
- [A function-local `static T` reproduces MSVC5's one-shot guarded magic-static init](function-local-static-magic-init.md) — c7 — cpp:static cpp:local | asm:test asm:call | topic:codegen-idiom — guard-bit test + conditional ctor + atexit around a built-once function-local object
- [Adjacent {lo:=clock, hi:=0} timer pair written lo-first is ONE 64-bit store](u64-store-clock-hi-zero.md) — c8 — cpp:store cpp:member | asm:mov | topic:codegen-idiom — two int stores get reordered (hi=0 before lo=eax), ~93-94% on the arm-a-countdown-timer pattern
- [Many-arg WinAPI call from a params struct: declare the struct in REVERSE arg order](struct-field-push-reverse-order.md) — c8 — cpp:struct | asm:push | topic:codegen-idiom — ascending struct-field loads [eax+0]..[eax+0x2c] each pushed; natural order ~97.6%
- [Inline .rdata jump-table data scored mismatched, code byte-exact](jumptable-data-overlap.md) — c7 — cpp:switch | asm:jmp | topic:scoring-artifact topic:tooling — low % on big switch, jump-table base reloc, $L labels
- [Sparse switch → cmp/je tree (100%); dense → jump table (~96%)](switch-cmpje-tree-vs-jumptable.md) — c8 — cpp:switch | asm:cmp asm:je asm:jmp | topic:codegen-idiom — same archetype, one sibling 100% other ~96%, cmp/je ladder vs jmp [tbl+eax*4]
- [Near-consecutive switch → running-subtract chain; if-else-if on `==` won't reproduce it](switch-subtract-chain-vs-ifelse.md) — c8 — cpp:switch cpp:branch | asm:sub asm:dec asm:je | topic:codegen-idiom — retail `sub imm,eax; je; dec; je; sub imm; jne` vs your three independent `cmp imm,eax; je`, ~47%→100% on switch rewrite; single-non-zero/multi-type ⇒ switch, ==0/==4 ⇒ if
- [switch case BODIES emitted in source-case order — reorder to retail layout](switch-cases-source-order.md) — c8 — cpp:switch | asm:jmp | topic:codegen-idiom — structure matches but case blocks in wrong .text order, fuzzy jumps after reorder
- [Large sparse switch: case DENSITY flips a packed byte-index table into a binary-search tree](switch-density-byte-index-table-vs-tree.md) — c9 — cpp:switch | asm:jmp asm:cmp asm:lea | topic:codegen-idiom — retail `lea eax,[x-lo];cmp eax,range;mov cl,[eax+T1];jmp[ecx*4+T2]` vs your `cmp mid;jg;je;...jmp[4*eax+T]` tree; a couple missing case labels cross MSVC5's density threshold. Extract T1/T2 from EXE for the exact case set + de-rotation map; minimal probe isolates the flip to the SET (HandleCommand 0x862f0: 115 cases tree, +2 → packs, +6%)
- [Pointer-returning switch w/ null default — pre-zero eax via an explicit result var](switch-pointer-default-result-var.md) — c8 — cpp:switch cpp:return | asm:xor asm:mov | topic:codegen-idiom — `return f()` per case keeps index in eax/no leading xor; `T* r=0; r=f(); break; return r;` forces edx index + xor eax,eax, ~51→70%
- [Switch range-checks emit unsigned `ja`/`jbe` only for an unsigned key](switch-key-unsigned-ja-vs-jg.md) — c9 — cpp:switch cpp:type | asm:ja asm:jbe asm:jg asm:jle | topic:codegen-idiom — body byte-exact but `cmp;ja/jbe` retail vs your `jg/jle`; type the switch-key field `u32` (matching-neutral), 97.86%→100% (InGame/Sirius WorkerHandlers state-tag pumps)
- [A scattered/incomplete unit usually plateaus per function; test TU-state exceptions](tu-completeness-rva-order.md) — c7 — topic:tu-layout | topic:scoring-artifact topic:wall topic:regalloc — whole unit 70-99% 0 exact, unit spans scattered RVAs; start with per-function reconstruction, but A/B-test predecessor state when an unchanged function moves
- [Simple TU reorder/removal is often byte-neutral; struct FIELD order is always load-bearing](within-tu-order-vs-field-order.md) — c8 — topic:tu-layout topic:scoring-artifact topic:regalloc | cpp:struct cpp:member — independent COMDATs make many small swap/remove probes neutral, but do not prove sibling-state independence; field reorder always moves member offsets
- [Adding a preceding function can re-color a later COMDAT through MSVC5 TU state](preceding-function-state-recolors-later-comdat.md) — c9 — cpp:method cpp:switch | asm:and asm:shl asm:mov | topic:regalloc topic:scheduling topic:tu-layout topic:codegen-idiom — unchanged later fn dips after a predecessor lands or is rehomed; localized register-color change is a reverse-search signal, and restoring real predecessors can recover it in reverse (BlitIntoDesc → ShadeRect; Teleporter → Update; SetFont → DrawGlyphRun)
- [A complex fn the /O2 optimizer gives up on emits a FRAMED all-memory body](o2-optimizer-bailout-framed.md) — c7 — cpp:local | asm:mov asm:sub | topic:wall topic:regalloc — push ebp/mov ebp,esp/sub esp,0x30 under /O2, every local [ebp-N], framed amid frameless siblings, your recompile comes out frameless
- [Unoptimized math helper: full /Od frame yet `__CIxxx` FP intrinsic ⇒ `/Od /Oi`](unoptimized-tu-with-intrinsics-od-oi.md) — c8 — cpp:local | asm:sub asm:idiv asm:call | topic:flags topic:codegen-idiom — unopt body (ebp frame, args through memory, plain `idiv`-by-const no magic, `mov reg,imm`) BUT `__CIpow`/`__ftol` FP-stack intrinsic; /O2 gives intrinsic+magic-div, /O1 gives idiv+`_pow` library — neither fits; own TU + `/Od /Oi` profile, ComputeCmdPercent 0x135110 34%→100%
- [/Od local slot ordering: identical instruction stream, permuted `[ebp-N]`](od-local-slot-ordering.md) — c6 — cpp:local | asm:mov | topic:wall topic:regalloc — /Od fn recompiles with byte-identical mnemonics/operands/frame-size but locals mapped to a permuted set of the same stack slots; ~85% byte / 99.5% fuzzy; same flat decl order doesn't fix (scope-nesting sensitive, unobservable from disasm), /Oi neutral; CFileImage RLE decoders 0x140aa0/0x140c50/0x145270/0x1453f0
- [A per-unit /Od profile masking a `#pragma optimize("", off)` island in an /O2 TU](od-island-pragma-optimize-off.md) - c9 - cpp:pragma cpp:flags | asm:ebp | topic:flags topic:codegen-idiom - fns matching ~99.5% only under an /Od unit profile while text-contained in a proven /O2 obj = a dev-source pragma island, NOT a per-file flag; fence with #pragma optimize("",off)/("",on) in the real TU (identical residual %, fileimagerundecode unit deleted, wave4-K)
- [Pre-loop member `=0` store + inline masked loop bound (don't fuse index-zero with member-zero / don't mask count in place)](zero-store-before-loop-inline-bound.md) — c8 — cpp:local cpp:loop | asm:xor asm:mov asm:and | topic:codegen-idiom topic:scheduling — 4-instr window before a counted loop permutes: `xor`vs`mov $0`, `and $0xff` in count reg vs a copy, `movw $0,[mem]` vs `movw reg,[mem]`; zero member FIRST + inline `(count & 0xff)` in the for-condition
- [Singly-walked CObList delete-loop rotates node through eax (twin copies) — regalloc wall](linked-list-walk-node-eax-rotation.md) — c7 — cpp:loop cpp:local | asm:mov asm:cmp | topic:wall topic:regalloc — list find+RemoveAt(+delete): retail head→eax + twin `mov ecx/esi,eax` copies + arg hoisted to edx; recompile keeps head/node in one callee-saved esi & derefs directly; FindInLists12 (no in-loop call) reaches 100%
- [`this` spilled to a fresh stack local + reloaded to seed a loop pointer (vs a register copy)](this-spilled-to-local-for-loop-seed.md) — c7 — cpp:loop cpp:local cpp:method | asm:push asm:mov | topic:wall topic:regalloc — `T* cur=this;` loop seed: retail reserves a `push ecx` slot + `mov edi,[esp+0x10]` (this reloaded), recompile does `mov edi,ebp`/`xor edi,edi` (no slot); body otherwise byte-identical, seed value is dead, non-steerable frame choice (VerifyBlockLinks @0x112c70 ~86%)
- [Singly-linked walk: advance the node BEFORE processing to avoid the loop peel](linked-list-advance-before-process.md) — c8 — cpp:loop cpp:local | asm:mov asm:test asm:jcc | topic:codegen-idiom — `node=node->next` read at loop bottom PEELS the first iter (body twice, ~46-63%); the advance-first `while(node){cur=node; node=node->next; …}` reads next at the top → single body, 100% (Method_030530)
- [Retry loop with early bail: top-tested `while` + `goto` (not `if{do-while}`, not `break`)](retry-loop-bail-while-goto-no-peel.md) — c8 — cpp:loop cpp:branch | asm:cmp asm:jcc asm:jmp | topic:codegen-idiom — lock/restore retry that bails past the success block: `if(c){do{…goto L;…}while(c)}` PEELS the first iter (~74%), `break` pins the loop var in a callee reg (~95%), top-`while`+`goto L` → single body + loop var dead on bail = 100% (Smacker RenderFrame @0x17caa0)
- [MFC map/list scan with early-return: top-tested `while(pos)`, NOT `if(pos)do{}while(pos)`](mfc-map-walk-while-not-guard-dowhile.md) — c9 — cpp:loop cpp:mfc cpp:branch | asm:test asm:jcc asm:call | topic:codegen-idiom — `GetStartPosition`+`GetNextAssoc` scan w/ early `return`: `if(pos)do{…}while(pos)` PEELS the body (~61%); top-`while(pos!=0)` rotates to single body; use real `GetStartPosition()` (not `GetCount()!=0?-1:0`+volatile) and declare `CObject* val` AFTER `CString key` so `val=0` stores an immediate not eax-from-scasb. HasKey* 61.36%→100% (supersedes the "loop-peel/zero-register-pinning wall")
- [Guarded counted loop: `for` floats the invariant preheader above the guard; `do-while` swaps the success/error exit order](loop-preheader-vs-exit-block-order.md) — c7 — cpp:loop cpp:branch cpp:eh | asm:lea asm:jcc asm:jmp | topic:wall topic:scheduling topic:regalloc — invariant `lea esi,[edi+N]` preheader + return-1-on-done/0-on-fail: `for` gives retail's success-first/`jl` order but hoists the leas above `i<count`; `if(c>0){leas;do{}while}` keeps them inside but error-first; prefer `for` (92.95% vs 90.7%), residual = preheader placement + `this`-register spill (SyncOptionsState @0x093170)
- [16-bit color pack via runtime RGB shift globals (`(c>>sar)<<off` per channel)](rgb-pack-variable-shift.md) — c7 — cpp:bitop | asm:sar asm:shl | topic:codegen-idiom topic:wall — `mov ecx,DAT_00683eac; sar ebp,cl; mov ecx,DAT_00683ea0; shl ebp,cl; or` packing 565/555 color; per-channel steerable, but ~22 packs sharing the 5 globals CSE+schedule into a ~70% wall (CLightFxRender::Shape1..8)
- [Constant-shift RGB pack via `mov ch,al; mov cl,bl` byte-insert](rgb-pack-byte-insert.md) — c7 — cpp:bitop | asm:mov asm:shl | topic:wall topic:regalloc — three `rand()%256` channels packed `(b<<16)|(g<<8)|r`; retail byte-inserts two channels into one 16-bit reg (`mov ch,al; mov cl,bl; shl ecx,8; or esi`) when g/b land in byte regs, recompile emits two `shl 8; or`; not steerable, ~70% complete+correct (CCreditsState::FlashColor 0x39d00)
- [`m_str = CString()` empty-assign diverges: retail inlines m_pchData=0, our MFC calls ??0CString](cstring-empty-init-version-divergence.md) — c7 — cpp:mfc cpp:ctor cpp:eh | asm:mov asm:call | topic:wall topic:eh — assigning a default-empty CString into a member lowers to operator= from a temp; retail builds the temp inline (mov [temp],0, MFC Init=_afxPchNil/0) while our packaged MFC's CString() Init=afxEmptyString is an out-of-line ??0CString call; small /GX setters plateau ~49% (CMulti::ClearString59c/5a0)
- [Pointer-walk loop: advance the cursor in the for-UPDATE, not via `*p++` in the body](pointer-walk-increment-in-for-update.md) — c8 — cpp:loop cpp:pointer | asm:push asm:add | topic:codegen-idiom — `f(p++,n)` materializes `mov eax,edi;push eax` + hoists `add edi,4` before the call; `for(p=base,i=0;i<N;i++,p++) f(p,n)` pushes the cursor reg directly + advances after the call (Serialize loops, 40→92%)
- [Accumulator `=0` BEFORE the cursor setup pins it in eax + frees the index reg](zero-init-before-pointer-setup-for-loop.md) — c8 — cpp:loop cpp:local | asm:xor asm:shl | topic:codegen-idiom — sum/accumulate loop seeded from `base+arg*stride`: declaring `sum=0` after the pointer keeps arg in eax + zeroes late; put `sum=0` FIRST so `xor eax,eax` is eager, arg lands in edx, edx reused as counter (SumWinRow 85→100%)
- [Read header fields THROUGH the advancing cursor, not via array index](header-fields-through-cursor-not-index.md) — c8 — cpp:pointer cpp:loop | asm:lea asm:add asm:mov | topic:codegen-idiom — RIFF/TLV parser reading `hdr[1]`/`hdr[2]` hoists them to direct `mov 0x8(ecx)` + folds the cursor to one `lea 0xc`; reading `*p;p++` through the loop cursor emits retail's `lea 0x4;add 4;mov (eax);add 4` walk (ParseWaveChunks 0x137110, 84→98%)
- [2D grid access: byte-offset address forces row-base then scaled index](byte-offset-address-row-base-scaled-index.md) — c8 — cpp:member cpp:array | asm:shl asm:lea asm:mov | topic:codegen-idiom — `m_grid[inner+outer*4]` fuses both dims into one lea+`*4` (wrong bytes + swapped reg); `*(i32*)((char*)m_grid + outer*0x10 + inner*4)` keeps `shl outer,4; lea rowbase; mov [+inner*4]` (GetFlag 86.5→100%)
- [Biased pointer advance in a loop: a single ternary, not load-then-conditional-fixup](biased-pointer-advance-ternary.md) — c8 — cpp:loop cpp:branch cpp:local | asm:je asm:jmp asm:xor | topic:codegen-idiom — intrusive chain walk `e=e->next; if(e) e-=K;` short-circuits the null case to the loop exit; `T* n=e->next; e = n ? n-K : n;` keeps retail's `je SETNULL; sub; jmp TEST; xor; TEST: test; jne` single loop-back (CHash Walk/FindInt 92-94→100%)
- [Counted-loop entry guard: `if (--count >= 0)` emits `dec eax; js` + `lea edi,[eax+1]` count recover](predecrement-guard-lea-recover-count.md) — c8 — cpp:loop cpp:branch cpp:local | asm:dec asm:js asm:lea | topic:codegen-idiom — `do-while` over `count` whose retail prologue guards with a bare `dec eax;js` BEFORE the reg saves then recovers the live counter `lea edi,[eax+1]` after: `count>0`/`count-->=1`/`for(i=count;i;i--)` all diverge (`test;jle` / extra `cmp` / pre-save push); spell `if(--count>=0){ i=count+1; do{}while(--i); }` (ApplyToRange @0x007c20 65/77/87.6→100%); thiscall-via-reg callback = PMF of a COMPLETE single-inheritance class
- [Count-down loop guarded by `mov eax,n; dec n; test eax; je; inc n` — spell `while (n-- != 0)`](test-old-value-decrement-loop-while-postdec.md) — c8 — cpp:loop cpp:local | asm:dec asm:test asm:inc | topic:codegen-idiom — load count, pre-decrement, test OLD value, re-increment on entry; `for(n;n;n--)`/`do{}while(--n)` emit plain `test;je` and miss the peel (zDArray member-ptr fixup 94→99.55%)
- [A vptr-stamping init that returns 0 (not `this`) is a void METHOD, not a ctor](vptr-stamp-void-init-not-ctor.md) — c8 — cpp:ctor cpp:member | asm:mov asm:xor | topic:codegen-idiom — `xor eax,eax; mov [ecx],&vftable; mov [ecx+N],eax; ret` keeps this in ecx / eax=0 / no mov eax,ecx; modeling a base-init as a real ctor adds mov eax,ecx (~73%), a void method matches (100%)
- [A polymorphic initializer with no vptr store and a boolean result is a method, not a constructor](no-vptr-bool-init-not-ctor.md) — c10 — cpp:ctor cpp:method cpp:virtual cpp:inheritance | asm:mov asm:ret | topic:codegen-idiom topic:identity — supposed standalone polymorphic ctor never stamps `[this]`, explicitly returns 0/1, callers test eax while preserving a derived vptr; restore inherited `Setup`, abstract base, and normal upcasts (CWwdGrid 74.15→79.55, caller→99.51, declared-only -1)
- [Copy an embedded struct member via `*p = *src` through a `&member` pointer to get lea+literal-zero](struct-copy-via-member-pointer-lea.md) — c7 — cpp:member cpp:local | asm:lea asm:mov | topic:codegen-idiom topic:regalloc — field-by-field box copy pins a zero reg (xor edx,edx reused) vs retail's lea edx,[ecx+N]+literal mov [ecx+M],0; whole-struct `*p=*src` keeps the lea pointer, 58%→100%
- [A signed `>>` lowers to `sar`; the unsigned cast that flips it to `shr` reschedules the block](signed-shift-cast-reschedules.md) — c7 — cpp:bitop cpp:shift | asm:sar asm:shr | topic:wall topic:regalloc — body byte-exact EXCEPT one `sar reg,N` vs target `shr reg,N` on a guarded-positive int `>>`; `(u32)x>>N` / unsigned param flips the opcode but swaps this/arg reg flow + lea<->shl (1 diff -> 11); keep the signed int form (zBitVec::SetSize 0x16e100)
- [Sentinel-seeding ctor: the last-arg (edx) store + the `-1` immediate store re-permute vs retail — scheduling wall](sentinel-seed-ctor-store-schedule.md) — c7 — cpp:ctor cpp:local | asm:mov | topic:wall topic:scheduling — tiny __thiscall ctor copying a few args then seeding `0x80000000`/`-1`/`0` sentinels; identical multiset, only the edx arg-store + the `-1` store float (source steers which arg→edx, NOT the store position), ~60% (CRemusNode @0x15b2c0)
- [Tiny multi-arg ctor/setter (no interleaved sentinels): assignment order fully steers the store schedule](arg-store-order-steers-schedule.md) — c8 — cpp:ctor cpp:local | asm:mov | topic:codegen-idiom topic:scheduling — steerable counterpart to sentinel-seed: arg-stores + vptr/const + trailing zeros, no `-1`/`0x80000000` interleaved; cl pins the 2nd-referenced arg in edx, write it second to reproduce retail's schedule (WorkerFull @0x15b300, CFaderMgr::SetConfig @0x17d980 → 100%)
- [A global referenced by-address is a `char[]` (array), not a `char*` (pointer)](global-by-address-array-not-pointer.md) — c8 — cpp:member cpp:cast | asm:mov | topic:codegen-idiom — default/fallback = ADDRESS of a writable global buffer emits `mov reg,OFFSET g` (b8/b9 imm32+DIR32); modeling the global as `char*` emits a 6-byte LOAD instead — declare it `char g[N]` so the name decays to its address (CContainerErr @0x16d9c0 97.1%→100%)
- [Retail keeps a dead global-read spill our MSVC5 DCE eliminates](dead-global-read-spill-dce.md) — c8 — cpp:local cpp:member | asm:mov asm:sub | topic:wall topic:regalloc — a fn reads two adjacent global fields (`g_gameReg->m_8c`/`->m_90`, viewport w/h) but uses one; retail keeps the unused load + spills it dead (`sub esp,8; mov [esp+N],reg`, per arm), our cl DCEs it + frees the reg → regalloc cascade; not steerable (named local/array elem/`(void)`/volatile all fail), accept plateau (CChatBoxOwner::Configure 0x20530 ~69%, ::HitTest 0x21140 ~38%)
- [`volatile` on a member reproduces retail's preserved redundant member-store](volatile-member-preserves-redundant-store.md) — c7 — cpp:member cpp:ctor | asm:mov | topic:codegen-idiom — retail keeps a dead `m=0;…;m=this` store (or writes one offset 3x to the same value) that our DCE drops + cascades the constant/reg pick; marking ONLY the duplicated member `volatile` keeps the stores in program order (literal still hoists), 75%→100% (CRemusReadStream init 0x1396f0); over-volatiling neighbours regresses; inline-helper folding does NOT survive DCE; steerable cousin of [[dead-global-read-spill-dce]]
- [Retail emits an unreachable dead BLOCK our MSVC5 DCE eliminates](dead-unreachable-recheck-block-dce.md) — c8 — cpp:branch cpp:switch cpp:member | asm:cmp asm:jne | topic:wall topic:regalloc — a `switch case N: if(m_flag!=0){big recheck}` where an early-out above the switch already proved `m_flag==0`; retail emits the whole dead recheck (`cmp cached,0; jne recheck`, 57-78 insns), our cl DCEs it so the case collapses to the default; control-flow (dead-BLOCK) cousin of [[dead-global-read-spill-dce]], caps the grunt AI steps 40-60s after the switch dispatch is exact (ChargeStep 0xef6b0 57.6%, UpdateArrival 0xf0130, ScanNearestTarget 0xf42f0)
- [Branchless `cond ? value : 0` mask: the neg/sbb/and destination register (eax vs edx) is a free-list pick](select-zero-mask-dest-register.md) — c7 — cpp:branch cpp:local | asm:neg asm:sbb asm:and | topic:wall topic:regalloc — `cond ? ptr : 0` arg lowers to neg/sbb/and mask; whole fn byte-exact except the masked result lands in edx (ours) vs eax (retail); inline ternary / explicit `(c?-1:0)&(i32)v` / named local / operand-swap all keep edx; ~99.3% (DSoundList::RemoveMatching 0x136f60)
- [Fully-unrolled Blowfish/Feistel block cipher — body byte-exact in shape, arg-pointer regalloc differs](blowfish-feistel-unroll-regalloc.md) — c7 — cpp:local cpp:loop | asm:mov asm:xor asm:shr | topic:wall topic:regalloc — 16 unrolled rounds, four S-box `mov reg,[idx*4+0x61aef8/b2f8/b6f8/baf8]`, P-array @0x61aeb0 pi-constants; identical instruction multiset but MSVC5 picks different callee-saved regs for the two u32* arg pointers (+ no P[17] pre-push hoist); alternating/temp-swap/deferred-read all 50-57%, logic provably correct (Blowfish_decipher 0x16fc70 ~56.6%, @early-stop)
- [Mirror-image twin functions schedule the same source differently — regalloc wall](mirror-function-divergent-schedule.md) — c8 — cpp:local | asm:xor asm:mov asm:shr | topic:wall topic:regalloc topic:scheduling — forward/reverse twins (encrypt/decrypt) over the SAME unrolled macro: MSVC5 picks a per-function regalloc, one twin 100% the other ~99.8% on a round-1 prologue permutation (extra `xor reg,reg` + `shr` via a different reg); XOR-order reshuffle doesn't move it (Blowfish_encipher 0x16f7f0 100% / Blowfish_decipher 0x16fc70 99.84%)
- [Inner-invariant `idx=(s*c)%m` hoisted + strength-reduced; retail re-reads `c` from memory](loop-invariant-multiply-strength-reduce-vs-memreread.md) — c7 — cpp:loop cpp:local | asm:imul asm:idiv asm:mov | topic:wall topic:regalloc topic:scheduling — nested loop with an inner-invariant `(s*count)%prime`: cl hoists it out + strength-reduces `s*count` to a spilled `+=count`, retail keeps count in MEMORY (re-read each outer iter) and recomputes `imul`/`idiv` inside; same pressure swaps modulus(ebp)<->counter(ebx); inline-recompute/`step=(i32)used` regress, logic correct (ScatterSamples 0x182940 ~59.2%, @early-stop)
- [Commutative `imul reg*mem`: cl loads the memory operand into eax, retail loads the register](commutative-imul-operand-in-eax.md) — c9 — cpp:loop cpp:local | asm:imul asm:mov | topic:wall topic:regalloc — grid/serialize index `j*m_c` (counter j in reg, m_c re-read via ebp base): retail `mov eax,edi;imul eax,[ebp]`, cl `mov eax,[ebp];imul eax,edi` - same 6 bytes, both re-read memory, only the operand-in-eax differs; not source-steerable (`j*m_c`/`m_c*j`/`i+m_c*j`/compound `idx=j;idx*=m_c` + permute all identical); distinct from the strength-reduce wall above (CMapMgr::Save 0x09f840 99.92% / Load 0x09f9a0 99.85%, @early-stop)
- [Identical switch arms sharing constants: cl hoists them to callee-saved regs, retail re-materializes per-arm](identical-switch-arm-constant-hoist.md) — c7 — cpp:switch cpp:local | asm:mov asm:or asm:lea | topic:wall topic:regalloc — jump-table whose every arm writes the SAME constant block ({-1,-1,1,1}/{0,0,0,0}): cl hoists -1/1/0 into callee-saved ebx/ebp/edi above the CString ctor (one esi-relative store/field) + uses a register for the `m=-1` member init, retail leaves the immediate store + re-materializes per arm via a region pointer (lea ebx,[esi+N];or eax,-1;mov [ebx],eax); pointer-vs-index spellings normalize identical, /O1 only swaps a wrong ebp frame; cl build-8034 heuristic (LoadVehicleGruntSprites 0x050ce0 ~37%, @early-stop)
- [Dense `switch` jump-table: MSVC `$Lnnn` symbol vs delinker inline-at-fn (~75-80% wall)](switch-jumptable-separate-comdat.md) — c8 — cpp:switch | asm:jmp asm:nop | topic:wall topic:scoring-artifact — a contiguous-case switch lowers to `jmp [eax*4+table]`; code byte-exact but MSVC emits the table as a separate `$L209` symbol (+ `8b ff` align filler) while the delinker inlines it into the fn at `+0x5c` (+ `90 90`), so the jmp's table reloc + table data don't pair, ≈79%; first FIX the bool tail (`case k: if(Fnk()) break; return 0; ... } return 1;` shared-`return 1`, NOT `return Fn()!=0` neg/sbb) which took CBzKindDispatch::Dispatch 0x37910 42%→79%
- [Unsigned `strlen(s) > N` reuses the inline-strlen `dec` flags for a free `js`](unsigned-strlen-compare-js-dec-flags.md) — c7 — cpp:builtin cpp:branch cpp:type | asm:js asm:cmp asm:jle | topic:wall — after `repne scasb;not ecx;dec ecx` retail splits the unsigned bound as `js Lfail; cmp ecx,N; jle Lok` (free sign-half off the live dec flags); `(i32)` cast emits signed `cmp;jle` minus the `js` (closest), `(u32)`/no-cast gives `jbe`, explicit `n<0||n>N` reschedules — not steerable (CNameRecord::SetNames 0x118040 ~96.6%, @early-stop)
- [MSVC5 won't value-init `T()` for a POD — give the temp an explicit zeroing ctor](vc5-no-value-init-explicit-zero-ctor.md) — c8 — cpp:ctor cpp:local cpp:new | asm:xor asm:mov | topic:codegen-idiom — pass-by-value default/zero temps (`Worker(…, T(), T(), …)` forwarder): VC5 leaves `T()` POD uninitialized so the recompile COPIES garbage into the blocks; add `T(): m0(0),m4(0),… {}` so each temp is zeroed in place via shared `xor` regs (CTileTriggerWiring::AddLogicDefaults 0x1163b0 27.85%→75.88%)
- [Reconstruct a carved EH funclet / mid-function fragment as a naked __asm body](naked-asm-funclet-reconstruction.md) — c9 — cpp:eh cpp:naked | asm:call asm:jmp asm:mov | topic:codegen-idiom topic:eh — a standalone target that is really a compiler EH funclet (dtor-unwind, catch, `mov fs:[0],ecx;leave;ret N` teardown), an atexit stub (`push <addr>;call atexit;pop ecx;ret`), or a `jmp short <outside>` shared-tail head; write `__declspec(naked)` + `__asm` with reloc-masked extern operands (`call Extern`/`mov reg,OFFSET data`/`push OFFSET fn`/`_emit 0xNN`); CLANG GOTCHA: `push OFFSET <data-extern>` segfaults the label parser — make the pushed label a function decl; 11 funclets + FaderDtor 0x17f570 → 100%
- [ILT jump-island to a __thiscall member: __asm can't name `Class::Method`, use a placeholder extern](ilt-thunk-to-method-asm-placeholder.md) — c9 — cpp:naked cpp:thunk | asm:jmp | topic:codegen-idiom topic:scoring-artifact — a 5-byte `e9 rel32` incremental-link island reconstructed as `__declspec(naked) { __asm jmp Target }`; free-function targets resolve exact (declare the exact sig; `struct HWND__;` spells HWND header-free, namespaces via `using`), but `jmp Class::Method` is C2420/C2400 (un-nameable) — jump through a reloc-masked placeholder extern (e9 byte exact, rel32 fully masked so the differing name doesn't block the match). CheckExePath/Eng_RegionCueA/StartupGate 0x2e6e/0x3936/0x2f59 + _MultiDispatch@12 0x3cab all exact
- [Naked-asm Win32 import call: `call dword ptr [iat-extern]`, not `call Import`](naked-asm-iat-indirect-call.md) — c9 — cpp:naked cpp:import | asm:call | topic:codegen-idiom topic:eh — in a `__declspec(naked)` body `call FreeLibrary` (even dllimport) emits `e8 <rel32 __imp>` but retail is `ff 15 <IAT>`; declare the IAT slot as `DATA(va) extern T(__stdcall* g_impX)(...)` and `call dword ptr [g_impX]`; also the framed `/GX` module carves bodies past a `mov eax,OFFSET h;call __EH_prolog` ebp prologue (FreeLibrary dtor 0x1d4a18 real entry 0x1d4a0d) or with the full FPO SEH frame inline (sound ctor 0x136fe0) — reproduce verbatim as naked asm; 0x1d4a18 + 0x136fe0 → 100%
- [Signed `% 2^k` lowers to abs-then-restore-sign (one cdq, two xor/sub around the mask)](signed-modulo-pow2-abs-restore.md) — c9 — cpp:modulo cpp:int | asm:cdq asm:xor asm:sub asm:and | topic:codegen-idiom — `cdq;xor eax,edx;sub eax,edx;and eax,0x7f;xor eax,edx;sub eax,edx` (single cdq reused) is signed `x % 128`, not `abs(abs(x)&0x7f)`; MSVC5 doesn't range-prove a non-negative operand; spell `(c & 0xff) % 128`, CNetSession::GetSlotPtr 0xc0430/ArmSlot 0xc03f0 → 100%
- [Force a remainder to reuse the quotient: write `n - q*C`, not `n % C` (which emits a fresh `div`)](unsigned-divmod-reuse-quotient.md) — c8 — cpp:modulo cpp:int | asm:mul asm:lea asm:div | topic:codegen-idiom — separate `*q=n/C; n=n%C;` emits magic-multiply for the quotient THEN a real `div` for the remainder; spell `unsigned q=n/C; *out=q; n-=q*C;` so cl strength-reduces `q*C` to a lea/shift chain and `add`s it back (matches retail's H:M:S splitter), TimeSplit 0x119210 52%→100%
- [Standalone /GX EH catch/unwind funclet — reconstruct verbatim as __declspec(naked)](naked-asm-eh-funclet-standalone.md) — c9 — cpp:eh cpp:naked | asm:mov asm:call asm:ret | topic:codegen-idiom — tiny carved symbol reads [ebp-N] with no push ebp, ends `mov eax,<mid-parent addr>;ret`; or `push <addr-past-self>;call atexit;pop ecx;ret`; naked must be a FREE fn (clang rejects naked members)
- [Multi-level /GX destructor: model the WHOLE base chain as a local polymorphic hierarchy (RTTI auto-namer names every ??_7)](eh-dtor-multilevel-polymorphic-chain.md) — c9 — cpp:dtor cpp:eh cpp:virtual | asm:mov asm:call | topic:codegen-idiom topic:eh — most-derived ~Dtor re-stamps N base vtables + calls N teardown helpers under a descending trylevel chain; frameless manual-vptr model ~34-43%; define the whole chain locally with inline virtual base dtors (each calls a reloc-masked teardown helper) + one out-of-line RVA-keyed leaf dtor -> MSVC folds the chain + emits the /GX frame, ??_7 auto-named (no RVA_COMPGEN pin); 9 SBI leaf dtors 43%->100%
- [Mark a NON-trivial, RVA-keyed mid-base dtor `inline` so thin derived dtors FOLD it (not CALL it)](inline-mid-base-dtor-folds-into-thin-derived.md) — c9 — cpp:dtor cpp:eh cpp:inline cpp:virtual | asm:mov asm:call | topic:codegen-idiom topic:eh — a self-RVA-keyed mid `~Mid` (full teardown) that thin `D : Mid` variants re-stamp + fold; out-of-line `Mid::~Mid` makes `~D` emit `call ??1Mid` (frame collapses ~53%), `inline` (still virtual→standalone COMDAT survives the RVA key) makes `~D` INLINE the retail double-pass; CWwdGameObject A/F/C 75/87/88→95.5/93.4/93.6, severus 2-level CSeverusWorkerX 87.8→100; non-RTTI vtable stamps still reloc-mask
- [Flag byte set after a memset: write `|=`, not `=` (read-modify-write)](flag-byte-or-after-memset.md) — c8 — cpp:struct cpp:local | asm:or asm:mov | topic:codegen-idiom — a `memset`'d packet/blob flag byte gets a bit set; retail reloads the zeroed byte + `or`s the bit in (source wrote `field |= 0x80`), not a direct `movb imm`; the direct store also re-permutes the neighbouring flag/id stores; CNetMgr::SaveConfig 0xbccd0 89.9%→96.4%
- [Folding a per-TU view onto its shared header: preserve the declaration position](fold-view-preserve-declaration-position.md) — c8 — cpp:member cpp:struct | topic:tu-layout topic:codegen-idiom topic:dedup — de-viewing a lone local shadow of an already-shared header (e.g. `CBankMgr`/`CResSource` view in CPlay.cpp → `<Gruntz/CBankMgr.h>`): a TOP-of-file `#include` reorders MSVC5's type tables and wobbles a matched neighbour (CPlay `LoadScrollSpeedOptions` 86.22→85.92) even for a byte-identical layout; put the `#include` IN-PLACE where the local view was so declaration order (and codegen) is preserved — neutral. Doesn't rescue divergent/heavy-header folds.
- [Shared-header declaration state re-colors an unrelated includer's /O2 schedule](header-fwd-decl-count-regalloc-butterfly.md) — c9 — cpp:struct cpp:header | asm:mov | topic:wall topic:regalloc topic:tu-layout topic:codegen-idiom — forward declarations, class definitions, and member-function signatures contribute to MSVC5 type-table state; edits can re-color unrelated functions in every direct/transitive includer. Proven firings include GameLevel.h fwd-decl count cratering `CSBI_MenuItem::DecCounter` 100→74 and the corrected `CGruntzMgr::ResetWorldState()` ABI moving three source-identical GruntzMgr.h consumers. Keep authentic declarations, preserve MAX, and use the includer intersection to diagnose/reverse similar dips.
- [zlib `send_bits` must be a MACRO (per-branch value re-subst + 16-bit `ush` accumulator)](zlib-send-bits-macro.md) — c8 — cpp:macro cpp:inline cpp:int | asm:shr asm:shl asm:and | topic:codegen-idiom topic:wall topic:tail-merge — reconstructing deflate's bit emitter (`_tr_align`/`compress_block`/…): model `bi_buf` as `u16` (16-bit `shr dx,cl` + direct accumulator byte-reads) and write `send_bits` as the verbatim `#define`, not an `__inline` fn — the macro re-substitutes `value` per branch so `g_code` loads once after the `cmp` + masks INSIDE the taken arm (inline collapses it to one pre-masked int hoisted above the branch). BitEmitMarker 0x188860 59%→87%(u16 acc)→96%(macro); residual = variable-length send_code `bi_valid += len` tail-merge (dest-register coin-flip, WALL)
- [A 2-sparse-case `switch` lowers to a compare CHAIN (no jump table) — case-order block layout](switch-two-sparse-cases-compare-chain.md) — c8 — cpp:switch cpp:branch | asm:cmp asm:je asm:jne | topic:codegen-idiom topic:tail-merge — a switch on sparse non-contiguous labels (`case 4`/`case 7`) is a compare chain `cmp K1;je L1;cmp K2;jne def;<K2 inline>;L1:<K1 out of line>` — the FIRST case out of line, SECOND inline (an if-ladder MIRRORS this, inlining the first arm → wall). REFUTES "a switch adds a jump table" (only dense ranges do). Also read the per-case guard polarity from the je/jne sense (`jne emit`⇒`if(guard==0)return 0` w/ return-0 inline). Method_02bfc0 0x2bfc0 if-ladder+`!=0` 80.7%→switch 95%→switch+`==0` 100%
- [Megafunction regalloc cascade: cached this-> locals beat the byte-correct reload spelling on fuzzy%](megafunction-cached-locals-vs-reload-regalloc.md) — c7 — cpp:local cpp:method | asm:mov asm:sub asm:add | topic:wall topic:regalloc — a big `this`-dispatcher pins {esi=this, edi=key, ebx=0, ebp=1} and RELOADS this->m_4/m_2dc per block; caching them as function-level locals scores HIGHER (register-operand diffs get partial credit) than inlining the reloads (extra `mov reg,[esi+N]` insert/delete against the diff), even though only the reload spelling recovers the true ebx=0/ebp=1 allocation. Frame size follows the allocation (retail `sub esp,0x10`, 2 top slots used) not the local count. CGamePlayInput::DispatchKey 0xcbcc0 (5850 B) 5.4%→78.5% locals / 74.1% both-inline; banked locals, deferred to final sweep.
- [`if (p) { delete p; p = 0; }` — cl elides delete's redundant null-guard; the zero-store is GUARDED](guarded-delete-null-store-elision.md) — c9 — cpp:dtor cpp:branch cpp:member | asm:test asm:je asm:call asm:mov | topic:codegen-idiom — owned-child teardown runs: a `Destroy(1)` vtable-view + guarded store recolors regalloc (fake "zero-pinning wall"), plain `delete p; p=0;` swaps each zero-store with the next load; retail is `if(p){delete p;p=0;}` — ONE test (delete's guard elided on the flow-known-non-null ptr), guarded store, true colors. CDDrawSurfaceMgr::Cleanup_155e20 96%-wall→100% EXACT
- [A 4-byte member-fn-pointer under an MI class means the PMF TYPE lives on an SI base](pmf-si-base-4byte-under-mi.md) — c9 — cpp:pmf cpp:inheritance | asm:mov asm:call | topic:codegen-idiom — converting a class to real multiple inheritance breaks every PMF table at once (stores gain a dword, dispatch gains a this-adjust); retail's RTTI names `zDArray<int (CUserLogic::*)(void)>` — declare the handler PMF on the SI PRIMARY BASE and cast `(Handler)&CLeaf::Method` at registration (~17 RunAct/RegisterActs pairs restored, CWapX conversion 2026-07-17)
