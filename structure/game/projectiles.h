#ifndef GAME_PROJECTILES_H
#define GAME_PROJECTILES_H

/*
 * Projectiles, explosives & particle effects — things that fly, explode, or
 * scatter: the generic projectile base, boomerangs, bombs/time bombs, explosions,
 * and the particle system.
 *
 * Provenance: ALL names from RTTI (mangled names kept). Name-only stubs (no layout recovered).
 * Corroborating: GAME_TIMEBOMB sound key (STRINGS_ANALYSIS.md §11).
 */

class CProjectile { /* .?AVCProjectile@@ */ };
class CBoomerang  { /* .?AVCBoomerang@@ */ };
class CTimeBomb   { /* .?AVCTimeBomb@@ */ };
class CExplosion  { /* .?AVCExplosion@@ */ };
class CParticlez  { /* .?AVCParticlez@@ */ };

#endif /* GAME_PROJECTILES_H */
